# 电商商品及评论存储规范手册 (实战开发版)

## 1. 文档概述
本手册旨在为开发人员提供明确的电商商品（业务类型_1）及其评论数据的存储标准。通过 **MySQL (核心结构化)**、**OSS (非结构化文件)**、**Milvus (语义向量)** 和 **Neo4j (实体关系)** 的协同工作，构建统一的知识中枢。

---

## 2. 数据分类与存储映射总表
所有数据项通过 `knowledge_id (全局知识ID)` 实现逻辑关联。

| 数据大类 | 具体数据项内容 | 存储位置 | 存储目的 |
| :--- | :--- | :--- | :--- |
| **基础核心数据** | 商品标题、价格、店铺、品牌等 | **MySQL** | 保证事务一致性，支持精确查询 |
| **非结构化文件** | 商品主图、详情图、抓取原始JSON/HTML | **OSS** | 低成本存储大文件，保留原始证据 |
| **语义特征数据** | 标题语义、功能卖点描述、评论倾向 | **Milvus** | 支持大模型语义检索 (RAG) |
| **知识关系网** | 品牌-商品、竞品对比、规格演进 | **Neo4j** | 支持复杂逻辑推理与关系挖掘 |
| **高频动态数据** | 实时库存、用户浏览足迹、临时缓存 | **NoSQL** | 解决高并发读写性能瓶颈 (详见第7章) |

---

## 3. MySQL 存储规范 (结构化核心)
**存储表名建议**: `EC_GOODS_PHONE (电商商品_手机表)`

### 3.1 商品基础表字段定义
| 字段名 (Field Name) | 数据类型 | 约束 | 中文备注 (Description) |
| :--- | :--- | :--- | :--- |
| `knowledge_id` | BIGINT | PK | 全局知识ID (各库关联索引) |
| `product_id` | VARCHAR(100) | Unique | 平台原始商品ID |
| `source_platform` | VARCHAR(20) | Not Null | 来源平台 (如: jd, taobao, pdd) |
| `title` | VARCHAR(500) | Not Null | 商品完整标题 |
| `price` | DECIMAL(12,2)| Not Null | 当前销售价格 |
| `currency_code` | VARCHAR(10) | Default 'CNY'| 货币代码 (如: CNY, USD) |
| `brand` | VARCHAR(100) | - | 品牌名称 |
| `shop_name` | VARCHAR(100) | Not Null | 店铺名称 |
| `spec_params` | JSON | - | 规格参数 (标准化后的键值对) |
| `comment_count` | INT | Default 0 | 评论总数 |
| `rating` | DECIMAL(3,1) | - | 商品评分 (0-5分) |
| `update_time` | DATETIME | - | 数据最后抓取/更新时间 |

---

## 4. OSS 存储规范 (非结构化资源)
**存储路径规范**: `/[business_type]/[date]/[knowledge_id]/`

| 文件标识 (File Label) | 文件命名规则 | 中文备注 (Description) |
| :--- | :--- | :--- |
| `original_json` | `raw_source.json` | 爬虫直接抓取的原始JSON数据 |
| `main_image` | `main_idx_0.jpg` | 商品主图 (第一张) |
| `detail_images` | `detail_[n].jpg` | 商品详情图集群 |
| `raw_html` | `page_snapshot.html` | 商品详情页网页快照 |

---

## 5. Milvus 存储规范 (语义向量库)
**向量维度建议**: 768维 (标准) 或 1536维 (高精度)

### 5.1 向量构建规则 (Embedding Rule)
为保证大模型检索效率，向量必须按以下文本顺序拼接后生成：
> **商品向量拼接顺序**: `[标题] + [品牌] + [分类] + [核心规格] + [功能摘要]`
> 
> **评论向量拼接顺序**: `[商品标题] + [评论关键词] + [情感倾向评分]`

### 5.2 字段说明
| 字段名 (Field Name) | 数据类型 | 中文备注 (Description) |
| :--- | :--- | :--- |
| `knowledge_id` | INT64 | 关联索引ID |
| `vector` | FLOAT_VECTOR | 语义特征向量数据 |
| `category_id` | INT64 | 业务分类标签 (用于加速分区过滤) |

---

## 6. Neo4j 存储规范 (知识图谱)
**存储目标**: 将零散的商品属性转化为有生命力的知识网。

### 6.1 节点 (Nodes) 与 关系 (Relationships) 定义
- **节点 (Node Labels)**:
    - `Product (商品)`: 属性包括 `knowledge_id`, `name`
    - `Brand (品牌)`: 属性包括 `name`
    - `Spec (规格)`: 属性包括 `key`, `value`
    - `Tag (标签)`: 属性包括 `name` (如: 续航王, 性价比高)

- **核心关系 (Relationship Types)**:
    - `(Product)-[:PRODUCED_BY (品牌所属)]->(Brand)`
    - `(Product)-[:HAS_SPEC (具备规格)]->(Spec)`
    - `(Product)-[:COMPETES_WITH (竞品关系)]->(Product)`: 基于价格带与规格自动建立
    - `(Product)-[:RECOM_BY (用户推荐)]->(Tag)`: 基于高频好评内容提取

---

## 7. 关于 NoSQL 的使用说明
在本项目架构中，**NoSQL (主要指 Redis 或 MongoDB)** 的引入需遵循以下原则：

### 7.1 是否使用 NoSQL？
**结论**: **建议使用 Redis 作为高性能缓存层，但在当前知识持久化层暂不以 MongoDB 为核心。**

### 7.2 详细原因说明 (Why/Why Not)
1. **为什么不将 MongoDB 作为主存储？**
    - 本项目已有 **OSS** 存储原始非结构化 JSON，且 **MySQL** 已经支持 JSON 类型字段存储标准化后的规格。
    - MongoDB 提供的灵活性在当前“MySQL+OSS”组合下并非核心痛点，且多增加一种数据库会提高运维复杂度和数据一致性维护成本。

2. **为什么需要 Redis？**
    - **大模型会话缓存**: 大模型在进行多轮对话时，频繁查询同一商品的知识。将跨越三库 (MySQL, Milvus, Neo4j) 拼装好的 **“知识上下文 JSON”** 存入 Redis，可使响应速度从秒级降至毫秒级。
    - **热点数据统计**: 如商品的实时浏览热度、抓取任务的排队状态等高频更新数据，适合存储在 Redis。

### 7.3 NoSQL 存储规则 (若使用 Redis)
| 键名格式 (Key Format) | 数据类型 | 中文备注 (Description) |
| :--- | :--- | :--- |
| `KB:CTX:[knowledge_id]` | STRING (JSON) | 拼装好的商品全量知识上下文 (供大模型直接使用) |
| `TASK:QUEUE:[platform]` | LIST | 待抓取的商品 ID 队列 |

---

## 8. 知识索引关联表 (Knowledge Index)
此表位于 MySQL，是三库同步的唯一依据。

| 字段名 (Field Name) | 数据类型 | 中文备注 (Description) |
| :--- | :--- | :--- |
| `knowledge_id` | BIGINT | 全局知识唯一ID (PK) |
| `mysql_ref` | VARCHAR(100) | 格式: `[表名]:[ID]` (指向结构化主数据) |
| `milvus_ref` | BIGINT | 指向向量库中的实体 ID |
| `neo4j_ref` | VARCHAR(100) | 指向图库中的节点 UUID |
| `oss_prefix` | VARCHAR(200) | 指向 OSS 资源文件夹路径 |
| `sync_status` | TINYINT | 同步状态: 0-未完成, 1-三库同步成功, 2-同步异常 |

---
**文档版本**: 1.1
**更新日期**: 2024-01-10
