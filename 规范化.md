# 网络数据抓取与清洗全流程实操指南（规范化落地版）

## 目录
- [一、核心前提：明确抓取与清洗的功能分割（各司其职）](#一核心前提明确抓取与清洗的功能分割各司其职)
- [二、元数据清单深度优化：核心业务类型结构化（字典定义）](#二元数据清单深度优化核心业务类型结构化字典定义)
  - [2.1 数据三级属性体系（升级：关联功能分割）](#21-数据三级属性体系升级关联功能分割)
  - [2.2 核心业务类型标准字典（落地核心）](#22-核心业务类型标准字典落地核心)
  - [2.3 优化后元数据清单字段（关联业务字典）](#23-优化后元数据清单字段关联业务字典)
  - [2.4 元数据清单示例（电商商品场景）](#24-元数据清单示例电商商品场景)
- [三、通用网络抓取场景全解析（基于业务字典的抓取落地）](#三通用网络抓取场景全解析基于业务字典的抓取落地)
  - [3.1 电商平台数据（最易入门，结构化程度高）](#31-电商平台数据最易入门结构化程度高)
  - [3.2 社交平台数据（半结构化为主，文本抓取核心）](#32-社交平台数据半结构化为主文本抓取核心)
  - [3.3 资讯平台数据（结构化+文本结合）](#33-资讯平台数据结构化文本结合)
  - [3.4 非结构化数据（图片/视频，基于字典的文本提取）](#34-非结构化数据图片视频基于字典的文本提取)
- [四、通用数据清洗场景全解析（基于抓取结果的规范化清洗）](#四通用数据清洗场景全解析基于抓取结果的规范化清洗)
- [五、数据清洗核心逻辑（小白必掌握）](#五数据清洗核心逻辑小白必掌握)
- [六、小白进阶路径（规范化流程落地导向）](#六小白进阶路径规范化流程落地导向)
- [附：小白常用工具清单（按功能分割分类）](#附小白常用工具清单按功能分割分类)

---

## 一、核心前提：明确抓取与清洗的功能分割（各司其职）
抓取与清洗是数据处理流程中两个**独立且衔接紧密**的核心环节，二者职责边界清晰，只有明确分割，才能提升流程效率、保证数据质量，避免职责混淆导致的流程混乱。

### 1.1 抓取环节的核心职责（前端数据获取与初步过滤）
抓取人员的核心目标是**“按要求获取有效原始数据，并完成初步过滤与格式转换”**，具体职责包括：
1.  基于明确的核心业务类型，确认待抓取数据的来源（如京东商品HTML、微博API、PDF文档）；
2.  明确当前业务类型下，可从来源中读取到的有效内容（如京东HTML可读取商品标题、价格、规格参数，无法读取用户隐私数据）；
3.  完成数据抓取：通过工具/代码获取原始数据（如爬取HTML、导出PDF、调用API）；
4.  初步过滤：剔除明显无效的原始数据（如空白页面、404错误数据、与业务类型无关的冗余内容）；
5.  格式转换：将抓取到的异构原始数据（HTML、PDF、图片）转换为**统一临时格式**（如JSON、CSV、TXT），便于后续清洗环节对接；
6.  元数据标注：按照核心业务类型，为临时数据标注对应的元数据（如业务类型ID、抓取日期、数据来源），并按指定路径存储。

### 1.2 清洗环节的核心职责（后端数据规范化与质量提升）
清洗人员的核心目标是**“基于业务字典与临时数据，完成数据规范化处理，输出可用数据”**，具体职责包括：
1.  对接抓取环节的临时数据与元数据，通过核心业务类型字典，明确数据清洗的目标、字段与标准；
2.  完成数据深度清洗：去重、字段标准化、缺失值处理、异常值修正；
3.  数据格式落地：按照业务字典定义的存储方式，将清洗后的数据转换为最终存储格式（如数据库表、结构化JSON、Excel）；
4.  数据质量校验：验证清洗后的数据是否符合业务字典的要求，输出数据质量报告；
5.  归档与交付：将合格数据按业务字典定义的路径归档，完成最终交付。

### 1.3 抓取与清洗的衔接核心（确保流程顺畅）
1.  核心衔接载体：**核心业务类型字典**（双方共用同一标准，避免认知偏差）；
2.  核心衔接格式：**统一临时数据格式**（JSON优先，小白友好、兼容性强）；
3.  核心衔接凭证：**元数据清单**（抓取人员填写，清洗人员参考，确保数据可追溯）。

---

## 二、元数据清单深度优化：核心业务类型结构化（字典定义）
核心目标：通过**核心业务类型标准字典**，实现“业务类型明确化→抓取行为标准化→清洗流程自动化→存储方式固定化”，解决数据处理流程中的混乱与低效问题。

### 2.1 数据三级属性体系（升级：关联功能分割）
| 层级         | 核心定义                          | 作用                          | 示例（网络可抓取数据）                          | 抓取环节职责                          | 清洗环节职责                          |
|--------------|-----------------------------------|-------------------------------|-----------------------------------------------|---------------------------------------|---------------------------------------|
| 一级：核心业务类型（最小颗粒度） | 数据对应的“具体实体/行为”，不可再拆分，以标准字典定义 | 明确抓取/清洗的核心目标与标准  | 电商商品、电商商品评论、社交平台评论、资讯文章、招聘岗位 | 按字典确认可抓取字段、生成临时数据    | 按字典确认清洗字段、标准化格式、落地存储 |
| 二级：数据载体类型                | 数据的物理存储形式                | 明确抓取/清洗的工具选择        | 网页HTML、JSON接口数据、PDF文档、JPG商品图、MP4短视频 | 按载体选择抓取工具、完成格式转换      | 按载体选择清洗工具、提取有效核心数据  |
| 三级：数据结构类型                | 数据的组织形式                    | 明确抓取/清洗的基础方法        | 结构化（JSON/CSV）、半结构化（HTML/XML）、非结构化（纯文本/图片/视频） | 按结构选择数据提取方式、生成统一临时结构 | 按结构选择数据清洗方式、转为结构化最终数据 |

### 2.2 核心业务类型标准字典（落地核心）
核心业务类型采用**键值对字典（JSON格式）**进行结构化定义，一份完整的业务类型字典包含“基础信息”“抓取配置”“清洗配置”“存储配置”四大模块，确保抓取与清洗人员有统一的执行标准，可直接复用、直接落地。

#### 2.2.1 字典通用模板（JSON格式）
```json
{
  "business_type_id": "唯一业务类型ID（如EC_GOODS_PHONE）",
  "business_type_name": "核心业务类型名称（如电商商品-手机类目）",
  "business_description": "业务类型描述（用于说明该业务类型的适用场景、数据用途）",
  "crawl_config": {
    "available_carrier": ["支持的数据源载体（如网页HTML、JSON接口）"],
    "must_crawl_fields": ["必须抓取的核心字段（如product_id、title、price）"],
    "optional_crawl_fields": ["可选抓取的扩展字段（如brand、ram、sales）"],
    "temp_data_format": "统一临时数据格式（如JSON/CSV）",
    "temp_data_save_path": "临时数据存储路径模板（如电商商品_手机类目/{crawl_date}/temp/）",
    "pre_filter_rule": "初步过滤规则（如剔除价格为空、商品ID重复的原始数据）"
  },
  "clean_config": {
    "must_clean_fields": ["必须清洗的核心字段（与必须抓取字段对应）"],
    "field_standard_rule": {
      "字段1": "标准化规则（如product_id：保留纯数字、去空格）",
      "字段2": "标准化规则（如price：去除¥/元、转为浮点型、剔除负值）",
      "字段3": "标准化规则（如crawl_time：统一转为YYYY-MM-DD HH:MM:SS格式）"
    },
    "missing_value_handle": {
      "核心字段": "处理策略（如删除/标注待补充）",
      "扩展字段": "处理策略（如填充未知/用众数补充）"
    },
    "abnormal_value_handle": "异常值统一处理规则（如剔除极端值、用中位数修正）"
  },
  "storage_config": {
    "final_data_format": "最终数据存储格式（如JSON/MySQL/Excel）",
    "final_data_save_path": "最终数据存储路径模板（如电商商品_手机类目/{crawl_date}/final/）",
    "database_config": {
      "table_name": "数据库表名（如jd_phone_goods）",
      "primary_key": "数据表主键（如product_id）",
      "field_mapping": "字段与数据库列的映射关系"
    }
  }
}