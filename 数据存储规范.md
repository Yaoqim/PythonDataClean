# 数据存储规范文档

## 文档概述

本规范定义了数据清洗MCP服务中数据存储的全生命周期管理规则，包括临时数据存储、元数据管理、清洗后数据存储等内容。本规范适用于所有业务类型的数据处理流程，确保数据一致性、可追踪性和数据质量。

---

## 第一部分：临时数据存储规范

### 1.1 临时数据文件结构

所有临时数据必须以**成对文件**形式存储在阿里云服务器上：

#### 1.1.1 成对文件组成

| 文件类型 | 说明 | 示例 |
| --- | --- | --- |
| **正式数据文件** | 包含实际的业务数据，格式为JSON、CSV等 | `EC_GOODS_PHONE_20240108_0001.json` |
| **元数据文件** | 与正式文件同名，后缀为`.metadata.json`，包含数据描述信息 | `EC_GOODS_PHONE_20240108_0001.metadata.json` |

#### 1.1.2 文件对应关系

```
数据文件:   EC_GOODS_PHONE_20240108_0001.json
元数据文件: EC_GOODS_PHONE_20240108_0001.metadata.json
↓
必须存储在同一目录下，清洗MCP自动匹配查找
```

### 1.2 文件命名规范

#### 1.2.1 命名规则

统一格式：`{业务类型}_{日期}_{序号}.{扩展名}`

| 组件 | 定义 | 说明 |
| --- | --- | --- |
| **业务类型** | 业务类型ID的大写形式 | EC_GOODS_PHONE、SOCIAL_COMMENT、NEWS_ARTICLE等 |
| **日期** | 数据抓取日期 | 格式为YYYYMMDD，如20240108 |
| **序号** | 同日期同类型的批次号 | 从0001开始，支持多批次处理 |
| **扩展名** | 文件格式 | json、csv、jsonl、parquet等 |

#### 1.2.2 命名示例

| 业务类型 | 完整文件名示例 |
| --- | --- |
| 电商商品 | `EC_GOODS_PHONE_20240108_0001.json` |
| 社交平台评论 | `SOCIAL_COMMENT_20240108_0001.jsonl` |
| 资讯文章 | `NEWS_ARTICLE_20240108_0001.jsonl` |
| 企业工商信息 | `ENTERPRISE_INFO_20240108_0001.json` |
| 招聘岗位 | `RECRUIT_JOB_20240108_0002.csv` |
| 房产信息 | `PROPERTY_INFO_20240108_0001.parquet` |

### 1.3 存储路径规范

#### 1.3.1 目录结构

```
/aliyun/data/
├── 2024/01/
│   ├── 08/                                           # 日期目录（YYYY/MM/DD）
│   │   ├── EC_GOODS_PHONE_20240108_0001.json
│   │   ├── EC_GOODS_PHONE_20240108_0001.metadata.json
│   │   ├── SOCIAL_COMMENT_20240108_0001.jsonl
│   │   ├── SOCIAL_COMMENT_20240108_0001.metadata.json
│   │   └── ...
│   └── 09/
│       └── ...
└── archive/                                          # 已处理/存档数据
    ├── 2023/
    └── 2024/
```

#### 1.3.2 路径规则

- **实时数据存储**：`/aliyun/data/{YYYY}/{MM}/{DD}/`
- **存档数据存储**：`/aliyun/data/archive/{YYYY}/{MM}/`
- **清洗失败数据**：`/aliyun/data/failed/{YYYY}/{MM}/{DD}/`

### 1.4 文件格式选择规范

根据业务特征和数据规模选择合适的存储格式：

| 数据特征 | 推荐格式 | 适用场景 | 优势 | 备注 |
| --- | --- | --- | --- | --- |
| 结构化、小规模(<10MB) | JSON | 企业工商、招聘岗位 | 格式简洁、易处理 | 存储体积较大 |
| 结构化、中等规模(10-500MB) | CSV/Parquet | 电商商品、房产信息 | Parquet压缩率高 | 需依赖库支持 |
| 结构化、大规模(>500MB) | Parquet/JSONL分片 | 房产评论、房产信息百万级 | 列式存储、高压缩率 | 不适合行级修改 |
| 半结构化、高频I/O | JSONL(行式JSON) | 社交评论、资讯文章 | 流式处理、行级独立 | 无法直接列式查询 |
| 非结构化、超大规模 | 分片+索引 | PDF财报、长文本 | 分片独立处理 | 需建立索引管理 |

### 1.5 临时数据生命周期管理

#### 1.5.1 生命周期流程

```
上传 → 清洗中(7天内) → 清洗完成/失败 → 保留1天 → 自动删除
  ↓                                    ↓
 元数据    清洗MCP处理                  可归档
```

#### 1.5.2 保留策略

| 数据状态 | 保留时间 | 处理方式 | 说明 |
| --- | --- | --- | --- |
| **处理中** | 7天 | 锁定文件，禁止删除 | 防止处理中文件被误删 |
| **清洗成功** | 1天 | 可选归档或删除 | 保持备份用途 |
| **清洗失败** | 7天 | 移至failed目录 | 保留以便问题排查 |
| **已归档** | 永久保存 | 定期备份检查 | 历史数据可查询 |

#### 1.5.3 自动清理规则

```python
# 伪代码示例
if file_last_modified_date < (today - 7 days):
    if file_status == "cleaned_success":
        # 可选：移至archive或删除
        move_to_archive(file) or delete_file(file)
    elif file_status == "cleaning":
        # 异常状态，需人工确认
        alert_admin(file)
    elif file_status == "cleaned_failed":
        # 保留失败文件便于问题排查
        pass
```

### 1.6 文件访问权限规范

| 角色/服务 | 读权限 | 写权限 | 删除权限 | 说明 |
| --- | --- | --- | --- | --- |
| 数据抓取MCP | ✗ | ✅ | ✗ | 仅创建临时数据文件 |
| 数据清洗MCP | ✅ | ✗ | ✗ | 仅读取文件进行处理 |
| 系统管理员 | ✅ | ✅ | ✅ | 仅限备份/恢复操作 |
| 其他服务 | ✗ | ✗ | ✗ | 禁止直接访问 |

---

## 第二部分：元数据字段详细说明

### 2.1 元数据文件结构

#### 2.1.1 完整元数据示例

```json
{
  "business_type_id": "EC_GOODS_PHONE",
  "business_type_name": "电商商品-手机",
  "data_structure_type": "结构化",
  "data_carrier_type": "JSON",
  "file_size_mb": 50,
  "estimated_record_count": 10000,
  "actual_record_count": 9856,
  "crawl_date": "20240108",
  "data_source": "京东商城",
  "crawl_tool": "Scrapy",
  "checksum": "abc123def456",
  "encoding": "UTF-8",
  "created_at": "2024-01-08T10:30:45Z",
  "created_by": "data_crawler_v1.0"
}
```

### 2.2 元数据字段详解

#### 2.2.1 核心字段说明

| 字段名 | 字段类型 | 必填 | 说明 | 取值范围/示例 |
| --- | --- | --- | --- | --- |
| **business_type_id** | string | 是 | 业务类型唯一标识 | 见第2.3节业务类型定义 |
| **business_type_name** | string | 是 | 业务类型的中文名称 | 电商商品-手机、社交平台评论等 |
| **data_structure_type** | string | 是 | 数据组织形式 | 结构化、半结构化、非结构化 |
| **data_carrier_type** | string | 是 | 数据物理存储形式 | JSON、CSV、XML、PDF、HTML等 |
| **file_size_mb** | number | 是 | 文件大小（单位MB） | 正整数，如50、100、500 |
| **estimated_record_count** | number | 是 | 预估记录数 | 正整数，用于优化批处理参数 |
| **actual_record_count** | number | 否 | 实际记录数 | 正整数，可为空（可在清洗后补充） |
| **crawl_date** | string | 是 | 数据抓取日期 | 格式YYYYMMDD，如20240108 |
| **data_source** | string | 是 | 数据来源名称 | 京东商城、微博、知乎、客户业务系统等 |
| **crawl_tool** | string | 否 | 数据抓取工具名称 | Scrapy、Selenium等网络爬虫，或"manual_import"表示客户手动导入 |
| **checksum** | string | 否 | 文件完整性校验值 | MD5或SHA256哈希值，可选用于数据完整性验证 |
| **encoding** | string | 否 | 文件字符编码 | 必须为UTF-8（强制要求），清洗时自动检查 |
| **created_by** | string | 否 | 数据来源系统/版本 | "data_crawler_v1.0"或"manual_upload"等标识，清洗时不需要此字段 |

### 2.3 业务类型ID定义

#### 2.3.1 业务类型分类体系

```
一级分类          二级分类              业务类型ID              中文名称
电商              商品信息              EC_GOODS_PHONE          电商商品-手机
                  商品信息              EC_GOODS_OTHER          电商商品-其他
                  用户评论              EC_COMMENT              电商商品评论
社交媒体          平台评论              SOCIAL_COMMENT          社交平台评论
                  用户内容              SOCIAL_POST             社交平台帖子
资讯媒体          文章内容              NEWS_ARTICLE            资讯文章
财务              财务数据              FINANCE_REPORT          财务报表
企业              工商信息              ENTERPRISE_INFO         企业工商信息
招聘              职位信息              RECRUIT_JOB             招聘岗位
视频              元数据                VIDEO_METADATA          短视频元数据
用户              用户档案              USER_PROFILE            用户基础信息
房产              房源信息              PROPERTY_INFO           房产信息
汽车              车型信息              CAR_INFO                汽车信息
供应链            订单信息              PURCHASE_ORDER          采购订单信息
```

#### 2.3.2 数据结构类型定义

| 数据结构类型 | 定义 | 对应文件格式 | 特征 | 清洗复杂度 |
| --- | --- | --- | --- | --- |
| **结构化** | 数据以表格或固定模式组织，字段定义明确、格式规范 | JSON、CSV、XML、SQL数据库 | 字段类型明确、易于解析、值域清晰 | ⭐⭐ |
| **半结构化** | 数据具有部分结构特征，某些字段结构化、某些字段文本化 | JSON混合、HTML表格、富文本 | 混合字段类型、需字段分离、结构文本共存 | ⭐⭐⭐ |
| **非结构化** | 数据以自由文本形式存储，无预定义字段和格式 | TXT、PDF、DOC、HTML正文 | 需NLP处理、信息提取困难、格式多样 | ⭐⭐⭐⭐ |

#### 2.3.3 数据载体类型定义

| 数据载体类型 | 定义 | 特征 | 处理难度 |
| --- | --- | --- | --- |
| **JSON** | JavaScript对象表示法，结构化键值对 | 易解析、支持嵌套、压缩率一般 | 低 |
| **CSV** | 逗号分隔值，表格格式 | 行列清晰、易分析、不支持嵌套 | 低 |
| **XML** | 可扩展标记语言，树型结构 | 格式严格、支持属性、冗长 | 中 |
| **HTML** | 超文本标记语言，网页格式 | 结构复杂、包含页面布局、需解析 | 中 |
| **PDF** | 便携式文档格式，排版固定 | 跨平台、难以解析、需OCR | 高 |
| **JSONL** | 行式JSON，每行一个JSON对象 | 流式处理、行级独立、易扩展 | 低 |
| **Parquet** | 列式存储格式 | 高压缩率、快查询、需专门库 | 中 |

# 清洗时元数据必填字段说明

清洗流程**必须**使用以下字段，其他字段为可选：

| 字段名 | 用途 | 必须性 |
| --- | --- | --- |
| **business_type_id** | 定位清洗规则 | ✅ 必须 |
| **business_type_name** | 日志记录、追踪 | ✅ 必须 |
| **data_structure_type** | 选择处理工具 | ✅ 必须 |
| **data_carrier_type** | 选择解析方式 | ✅ 必须 |
| **file_size_mb** | 优化批处理参数 | ✅ 必须 |
| **estimated_record_count** | 优化批处理参数 | ✅ 必须 |
| **crawl_date** | 记录数据时间属性 | ✅ 必须 |
| **data_source** | 数据来源追踪 | ✅ 必须 |
| **crawl_tool** | 信息参考，清洗不需要 | ❌ 可选 |
| **checksum** | 完整性校验，清洗不需要 | ❌ 可选 |
| **encoding** | 文件必须为UTF-8，无需检查 | ❌ 可选 |
| **created_by** | 信息参考，清洗不需要 | ❌ 可选 |

#### 2.4.1 必填字段校验

```python
# 清洗时真正的必填字段校验（不包括可选字段）
required_fields = [
    'business_type_id',
    'business_type_name', 
    'data_structure_type',
    'data_carrier_type',
    'file_size_mb',
    'estimated_record_count',
    'crawl_date',
    'data_source'
]

def validate_metadata(metadata):
    for field in required_fields:
        if field not in metadata or metadata[field] is None:
            raise ValueError(f"缺少必填字段: {field}")
    
    # 编码必须是UTF-8
    encoding = metadata.get('encoding', 'UTF-8').upper()
    if encoding != 'UTF-8':
        raise ValueError(f"文件编码必须为UTF-8，不能使用{encoding}")
```

#### 2.4.2 字段值校验规则

| 字段名 | 校验规则 | 错误处理 |
| --- | --- | --- |
| **business_type_id** | 必须与支持的业务类型ID匹配 | 拒绝处理，记录错误 |
| **data_structure_type** | 必须为：结构化、半结构化、非结构化 | 拒绝处理 |
| **data_carrier_type** | 必须为：JSON、CSV、XML、PDF、HTML、JSONL、Parquet | 拒绝处理 |
| **file_size_mb** | 必须为正整数，不超过5000MB | 拒绝处理 |
| **estimated_record_count** | 必须为正整数 | 使用1作为默认值 |
| **crawl_date** | 格式YYYYMMDD，日期有效性校验 | 拒绝处理 |

---

## 第三部分：清洗后数据存储规范

### 3.1 清洗后数据存储方式

所有清洗后的数据必须存储到关系型数据库中，不返回文件给工作流。

#### 3.1.1 存储策略（基于数据量级分类）

根据各业务类型的数据量级和特征，采用分层存储策略：

| 数据量级 | 存储策略 | 表设计 | 应用场景 | 业务类型示例 |
| --- | --- | --- | --- | --- |
| **小规模** | 统一单表 | 各类型合并为一个表 | 数据量<10万条/年 | 企业工商、财务报表、用户档案 |
| **中规模** | 业务类型独立表 | 按业务类型创建单独表 | 数据量10-100万条/年 | 电商商品、招聘岗位、汽车信息、短视频 |
| **大规模** | 业务类型+分片表 | 按业务类型+时间/维度分片 | 数据量100-1000万条/年 | 电商评论、房产信息 |
| **超大规模** | 分布式+分片表 | 按业务类型+多维分片 | 数据量>1000万条/年 | 社交评论、社交帖子 |

#### 3.1.2 表名命名规范

**原则**：小驼峰格式，体现业务类型和分片维度

```
【小规模数据】统一基础表（所有类型合并）
basic_entity_info            # 基础实体信息（企业、用户、财务等）

【中规模数据】业务类型独立表
ec_goods_info               # 电商商品信息
recruit_job_info            # 招聘岗位
video_metadata_info         # 短视频元数据
car_info                    # 汽车信息

【大规模数据】业务类型+时间分片表
ec_comment_2024           # 电商评论 - 2024年
ec_comment_2025           # 电商评论 - 2025年
purchase_order_2024       # 采购订单 - 2024年
purchase_order_2025       # 采购订单 - 2025年

【超大规模数据】业务类型+年份分片表
social_comment_2024_00  # 社交评论 - 2024年 片0
social_comment_2024_01  # 社交评论 - 2024年 片1
social_post_2024_00     # 社交帖子 - 2024年 片0
social_post_2024_01     # 社交帖子 - 2024年 片1
social_comment_2025_00  # 社交评论 - 2025年 片0
```

**表名映射关系**：

| 业务类型 | 数据量级 | 表名 | 说明 |
| --- | --- | --- | --- |
| EC_GOODS_PHONE | 中等 | ec_goods_info | 所有商品统一表（按category字段区分手机/电脑/靴等） |
| EC_COMMENT | 大 | ec_comment_YYYY | 按年分片（同步调整为按年） |
| SOCIAL_COMMENT | 特大 | social_comment_YYYY_XX | 按年份+分片号分片（改为按年） |
| SOCIAL_POST | 特大 | social_post_YYYY_XX | 按年份+分片号分片（改为按年） |
| NEWS_ARTICLE | 中等 | news_article_info | 单表存储 |
| FINANCE_REPORT | 小 | financial_report | 独立表 |
| ENTERPRISE_INFO | 小 | company_info | 独立表 |
| RECRUIT_JOB | 中等 | recruit_job_info | 单表存储 |
| VIDEO_METADATA | 中等 | video_metadata_info | 单表存储 |
| USER_PROFILE | 小 | user_profile | 独立表 |
| PROPERTY_INFO | 中等 | property_info | 单表或按年分片（字典型数据，更新并不是新增） |
| CAR_INFO | 中等 | car_info | 单表存储 |
| PURCHASE_ORDER | 大 | purchase_order_YYYY | 按年分片 |

### 3.2 分层存储策略详解

#### 3.2.1 小规模数据策略（<10万条/年）

**适用业务类型**：企业工商、财务报表、用户档案

**存储方案**：根据业务类型的不同字段定义，拆分为三个独立表进行存储。不能合并，因为各业务类型的字段结构差异很大。

**表1：company_info（企业工商信息）**

根据《业务类型_7企业工商信息.md》的字段定义

```sql
CREATE TABLE company_info (
    -- 主键
    company_code VARCHAR(50) PRIMARY KEY COMMENT '社会信用代码',
    
    -- 核心字段
    company_name VARCHAR(255) NOT NULL COMMENT '企业名称',
    registered_capital DECIMAL(15, 2) COMMENT '注册资本',
    establishment_date DATE COMMENT '成立日期',
    legal_person VARCHAR(100) COMMENT '法定代表人',
    business_scope LONGTEXT COMMENT '经营范围',
    registered_address VARCHAR(500) COMMENT '注册地址',
    operating_status VARCHAR(50) COMMENT '经营状态',
    industry_category VARCHAR(100) COMMENT '行业分类',
    
    -- 准确度评估字段
    company_name_anal INT CHECK(company_name_anal >= 0 AND company_name_anal <= 100) COMMENT '企业名称准确度评分(0-100)',
    company_name_anal_info VARCHAR(500) COMMENT '企业名称准确度评估说明',
    company_code_anal INT CHECK(company_code_anal >= 0 AND company_code_anal <= 100) COMMENT '社会信用代码准确度评分(0-100)',
    company_code_anal_info VARCHAR(500) COMMENT '社会信用代码准确度评估说明',
    registered_capital_anal INT CHECK(registered_capital_anal >= 0 AND registered_capital_anal <= 100),
    registered_capital_anal_info VARCHAR(500),
    operating_status_anal INT CHECK(operating_status_anal >= 0 AND operating_status_anal <= 100),
    operating_status_anal_info VARCHAR(500),
    
    -- 时间字段
    data_crawl_date DATE NOT NULL COMMENT '数据抓取日期',
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '清洗完成时间',
    imported_at TIMESTAMP COMMENT '数据导入时间',
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE COMMENT '数据是否有效',
    quality_score INT CHECK(quality_score >= 0 AND quality_score <= 100) COMMENT '数据质量评分',
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid' COMMENT '数据处理状态',
    
    -- 版本控制
    version INT DEFAULT 1 COMMENT '数据版本号',
    
    -- 索引
    KEY idx_company_name (company_name),
    KEY idx_crawl_date (data_crawl_date),
    KEY idx_status (data_status),
    KEY idx_company_code_anal (company_code_anal)
);
```

**表2：user_profile（用户档案）**

根据《业务类型_10用户基础信息.md》的字段定义

```sql
CREATE TABLE user_profile (
    -- 主键
    user_id VARCHAR(100) PRIMARY KEY COMMENT '用户全局唯一ID',
    
    -- 核心字段
    username VARCHAR(255) NOT NULL COMMENT '用户名',
    user_level INT COMMENT '用户等级',
    location VARCHAR(255) COMMENT '所在地区',
    registration_date DATE COMMENT '注册日期',
    follower_count INT COMMENT '粉丝数量',
    following_count INT COMMENT '关注数量',
    bio LONGTEXT COMMENT '用户简介',
    avatar_url VARCHAR(500) COMMENT '头像URL',
    
    -- 准确度评估字段
    user_id_anal INT CHECK(user_id_anal >= 0 AND user_id_anal <= 100) COMMENT '用户ID准确度评分',
    user_id_anal_info VARCHAR(500) COMMENT '用户ID准确度评估说明',
    username_anal INT CHECK(username_anal >= 0 AND username_anal <= 100) COMMENT '用户名准确度评分',
    username_anal_info VARCHAR(500) COMMENT '用户名准确度评估说明',
    follower_count_anal INT CHECK(follower_count_anal >= 0 AND follower_count_anal <= 100),
    follower_count_anal_info VARCHAR(500),
    user_level_anal INT CHECK(user_level_anal >= 0 AND user_level_anal <= 100),
    user_level_anal_info VARCHAR(500),
    
    -- 时间字段
    data_crawl_date DATE NOT NULL COMMENT '数据抓取日期',
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '清洗完成时间',
    imported_at TIMESTAMP COMMENT '数据导入时间',
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE COMMENT '数据是否有效',
    quality_score INT CHECK(quality_score >= 0 AND quality_score <= 100) COMMENT '数据质量评分',
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid' COMMENT '数据处理状态',
    
    -- 版本控制
    version INT DEFAULT 1 COMMENT '数据版本号',
    
    -- 索引
    KEY idx_username (username),
    KEY idx_crawl_date (data_crawl_date),
    KEY idx_status (data_status),
    KEY idx_user_level_anal (user_level_anal)
);
```

**表3：financial_report（财务报表）**

根据《业务类型_6财务报表.md》的字段定义

```sql
CREATE TABLE financial_report (
    -- 复合主键
    company_code VARCHAR(50) NOT NULL COMMENT '企业社会信用代码',
    report_period VARCHAR(50) NOT NULL COMMENT '报告期（如：2024Q1、2023年）',
    publish_date DATE NOT NULL COMMENT '报表发布日期',
    PRIMARY KEY (company_code, report_period, publish_date),
    
    -- 核心字段
    revenue DECIMAL(15, 2) COMMENT '营业收入',
    net_profit DECIMAL(15, 2) COMMENT '净利润',
    total_assets DECIMAL(15, 2) COMMENT '总资产',
    total_liabilities DECIMAL(15, 2) COMMENT '总负债',
    shareholders_equity DECIMAL(15, 2) COMMENT '股东权益',
    earnings_per_share DECIMAL(10, 4) COMMENT '每股收益',
    operating_cash_flow DECIMAL(15, 2) COMMENT '经营现金流',
    
    -- 准确度评估字段
    revenue_anal INT CHECK(revenue_anal >= 0 AND revenue_anal <= 100) COMMENT '营业收入准确度评分',
    revenue_anal_info VARCHAR(500) COMMENT '营业收入准确度评估说明',
    net_profit_anal INT CHECK(net_profit_anal >= 0 AND net_profit_anal <= 100) COMMENT '净利润准确度评分',
    net_profit_anal_info VARCHAR(500) COMMENT '净利润准确度评估说明',
    total_assets_anal INT CHECK(total_assets_anal >= 0 AND total_assets_anal <= 100),
    total_assets_anal_info VARCHAR(500),
    earnings_per_share_anal INT CHECK(earnings_per_share_anal >= 0 AND earnings_per_share_anal <= 100),
    earnings_per_share_anal_info VARCHAR(500),
    
    -- 时间字段
    data_crawl_date DATE NOT NULL COMMENT '数据抓取日期',
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '清洗完成时间',
    imported_at TIMESTAMP COMMENT '数据导入时间',
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE COMMENT '数据是否有效',
    quality_score INT CHECK(quality_score >= 0 AND quality_score <= 100) COMMENT '数据质量评分',
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid' COMMENT '数据处理状态',
    
    -- 版本控制
    version INT DEFAULT 1 COMMENT '数据版本号',
    
    -- 索引
    KEY idx_company_code (company_code),
    KEY idx_report_period (report_period),
    KEY idx_publish_date (publish_date),
    KEY idx_crawl_date (data_crawl_date),
    KEY idx_status (data_status),
    KEY idx_revenue_anal (revenue_anal)
);
```

**存储特性**：
- 三个业务类型各自独立表，字段完全不同
- 企业表以company_code为主键
- 用户表以user_id为主键
- 财务表以(company_code, report_period, publish_date)为复合主键
- 每个关键字段都配有准确度评估字段（_anal和_anal_info）
- 支持精细化的数据质量管理

---

#### 3.2.2 中规模数据策略（10-100万条/年）

**适用业务类型**：电商商品、招聘岗位、汽车信息、短视频元数据、资讯文章

**存储方案**：按业务类型创建独立表，**同一类型内的细分（如手机/电脑/鞋）通过字段区分，不分开建表**。

```sql
-- 电商商品表（包含手机、电脑、鞋、衣服等所有商品）
CREATE TABLE ec_goods_info (
    product_id VARCHAR(50) PRIMARY KEY,
    product_name VARCHAR(255) NOT NULL,
    category VARCHAR(50) NOT NULL,      -- 手机 / 电脑 / 鞋 / 衣服等（通过此字段区分）
    subcategory VARCHAR(100),           -- 进一步细分（如：手机->iPhone/安卓)
    platform VARCHAR(50),               -- 数据来源平台
    price DECIMAL(10, 2),
    description LONGTEXT,
    
    -- 时间字段
    data_crawl_date DATE NOT NULL,
    data_update_time TIMESTAMP,
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    imported_at TIMESTAMP,
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE,
    quality_score INT CHECK(quality_score >= 0 AND quality_score <= 100),
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid',
    validity_reason VARCHAR(500),
    
    -- 版本控制
    version INT DEFAULT 1,
    
    -- 索引（重点：category字段方便按商品类型查询）
    KEY idx_category (category),
    KEY idx_subcategory (subcategory),
    KEY idx_platform (platform),
    KEY idx_crawl_date (data_crawl_date),
    KEY idx_status (data_status)
);
```

**优势**：
- 单表足以容纳数据，性能高效
- 通过category字段灵活查询任意商品类型
- 易于维护和扩展

**查询示例**：
```sql
-- 查询所有手机
SELECT * FROM ec_goods_info WHERE category = '手机';

-- 查询所有电脑
SELECT * FROM ec_goods_info WHERE category = '电脑';

-- 查询特定商品类型的有效数据
SELECT * FROM ec_goods_info WHERE category = '手机' AND data_status = 'valid';
```

---

#### 3.2.3 大规模数据策略（100-1000万条/年）

**适用业务类型**：电商评论、房产信息、采购订单

**存储方案**：按业务类型创建主表，按**时间分片**（按月或按日）。

**表命名规则**：`{业务类型}_{YYYYMM}` 或 `{业务类型}_{YYYYMMDD}`

```sql
-- 电商评论表 - 按月分片
-- 表1：ec_comment_202401（2024年1月）
CREATE TABLE ec_comment_202401 (
    comment_id VARCHAR(100) PRIMARY KEY,
    product_id VARCHAR(50) NOT NULL,
    user_id VARCHAR(100),
    rating INT CHECK(rating >= 1 AND rating <= 5),
    comment_text TEXT,
    
    -- 时间字段
    data_crawl_date DATE NOT NULL,
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    imported_at TIMESTAMP,
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE,
    quality_score INT,
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid',
    
    -- 索引
    KEY idx_product_id (product_id),
    KEY idx_user_id (user_id),
    KEY idx_rating (rating)
);

-- 表2：ec_comment_202402（2024年2月）
-- 结构相同，数据按日期分片存储
```

**分片策略**：
- 按月分片：每个月的数据存储在一个独立表中
- 分片表可独立查询、备份、删除
- 查询跨月份数据时通过UNION ALL查询多张表

**查询示例**：
```sql
-- 查询2024年1月的评论
SELECT * FROM ec_comment_202401 WHERE product_id = 'PROD_123';

-- 查询2024年全年的评论（跨分片）
SELECT * FROM ec_comment_202401
UNION ALL SELECT * FROM ec_comment_202402
-- ... 其他月份
WHERE product_id = 'PROD_123';

-- 使用分区表简化跨分片查询（MySQL 5.1+）
-- 创建分区表（推荐）
CREATE TABLE ec_comment_all (
    comment_id VARCHAR(100),
    product_id VARCHAR(50),
    -- ... 其他字段 ...
    data_crawl_date DATE
) PARTITION BY RANGE (YEAR(data_crawl_date) * 100 + MONTH(data_crawl_date)) (
    PARTITION p202401 VALUES LESS THAN (202402),
    PARTITION p202402 VALUES LESS THAN (202403)
);
```

**优势**：
- 每个月的表规模可控（~100-1000万条）
- 支持并行查询和独立维护
- 删除过期数据简单高效（直接DROP表）
- 性能稳定，不会因数据增长导致查询变慢

---

#### 3.2.4 超大规模数据策略（>1000万条/年）

**适用业务类型**：社交评论、社交帖子

**存储方案**：按业务类型+年份分片+多分片号

**表命名规则**：`{业务类型}_{YYYY}_{分片号}`（改为按年分片，而非按日期分片）

```sql
-- 社交评论表 - 按年份+分片号分片
-- 表1：social_comment_2024_00（2024年第0片）
-- 表2：social_comment_2024_01（2024年第1片）
-- 表3：social_comment_2024_02（2024年第2片）
-- 表4：social_comment_2025_00（2025年第0片）
-- ...

CREATE TABLE social_comment_2024_00 (
    comment_id VARCHAR(100) PRIMARY KEY,
    post_id VARCHAR(100) NOT NULL,
    user_id VARCHAR(100),
    content LONGTEXT,
    likes_count INT,
    reply_count INT,
    
    -- 时间字段
    data_crawl_date DATE NOT NULL,
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    imported_at TIMESTAMP,
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE,
    quality_score INT,
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid',
    
    -- 索引
    KEY idx_post_id (post_id),
    KEY idx_user_id (user_id),
    KEY idx_likes (likes_count)
);
```

**分片策略**：
- **时间维度**：按年分片（每年一套表或多个表）
- **数据维度**：同一年的数据按分片号进一步分片（如00/01/02...表示第0/1/2...片）
- 分片号通常基于 `hash(comment_id) % 分片数` 计算

**分片数量计算**：
```
年度数据量 / 单表容纳上限 = 分片数
例如：社交平台每年10亿条评论，单表承载1亿条
分片数 = 10亿 / 1亿 = 10片（social_comment_2024_00/01/.../09）
```

**写入策略**：
```python
# Python伪代码
def determine_table_name(record, business_type, crawl_date):
    # 从crawl_date中提取年份
    year = crawl_date[:4]  # 例如：'2024'
    
    # 计算分片号（基于comment_id的hash）
    shard_id = hash(record['comment_id']) % TOTAL_SHARDS  # 假设TOTAL_SHARDS=10
    
    # 组合表名
    table_name = f"{business_type}_{year}_{shard_id:02d}"
    # 例如：social_comment_2024_00
    
    return table_name

# 使用示例
for record in daily_comments:
    table = determine_table_name(record, 'social_comment', '20240101')
    db.insert(table, record)  # 插入到对应分片表
```
```

**优势**：
- 支持PB级数据存储
- 单表规模可控，查询性能恒定
- 支持并行写入和查询
- 易于分布式扩展

**查询示例**：
```sql
-- 查询2024年中特定帖子的所有评论（需查询多个分片）
SELECT * FROM social_comment_2024_00 WHERE post_id = 'POST_123'
UNION ALL SELECT * FROM social_comment_2024_01 WHERE post_id = 'POST_123'
UNION ALL SELECT * FROM social_comment_2024_02 WHERE post_id = 'POST_123'
... 一直到 social_comment_2024_09;

-- 查询2025年的最新评论（跨年查询）
SELECT * FROM social_comment_2025_00 WHERE post_id = 'POST_456' AND data_crawl_date >= '2025-01-01';

-- 查询不同年份的高质量评论
-- 2024年
(SELECT * FROM social_comment_2024_00 WHERE quality_score >= 85)
UNION ALL
(SELECT * FROM social_comment_2024_01 WHERE quality_score >= 85)
...
UNION ALL
-- 2025年
(SELECT * FROM social_comment_2025_00 WHERE quality_score >= 85);
```

---

### 3.3 清洗后数据的时间维度管理

#### 3.3.1 时间戳字段规范

所有清洗后的数据表必须包含以下时间相关字段：

| 字段名 | 字段类型 | 说明 | 默认值/赋值方式 |
| --- | --- | --- | --- |
| **data_crawl_date** | DATE | 原始数据抓取日期 | 从元数据中获取 |
| **data_update_time** | TIMESTAMP | 原始数据更新时间 | 从原数据中提取或使用爬取时间 |
| **cleaned_at** | TIMESTAMP | 清洗完成时间 | 系统当前时间 |
| **imported_at** | TIMESTAMP | 数据导入数据库时间 | 系统当前时间 |
| **last_updated_at** | TIMESTAMP | 最后更新时间 | 初始等于imported_at，后续更新时修改 |

#### 3.3.2 时间戳赋值规则

```python
# 时间戳赋值示例
from datetime import datetime

cleaned_record = {
    # ... 其他字段 ...
    'data_crawl_date': datetime.strptime(metadata['crawl_date'], '%Y%m%d').date(),
    'data_update_time': original_data.get('update_time') or datetime.now(),
    'cleaned_at': datetime.now(),
    'imported_at': datetime.now(),
    'last_updated_at': datetime.now()
}
```

### 3.4 数据有效性规范

#### 3.4.1 数据有效性标记字段

所有清洗后的数据表应包含以下有效性相关字段：

| 字段名 | 字段类型 | 说明 | 取值 |
| --- | --- | --- | --- |
| **is_valid** | BOOLEAN | 数据是否有效 | true/false |
| **validity_reason** | VARCHAR | 无效的原因说明 | null表示有效，或具体原因文本 |
| **quality_score** | INT(0-100) | 数据质量评分 | 0-100 |
| **data_status** | ENUM | 数据处理状态 | valid、pending_review、invalid |

#### 3.4.2 数据质量评分规范

| 分数范围 | 等级 | 处理方式 | 说明 |
| --- | --- | --- | --- |
| ≥ 85 | 优秀(Excellent) | ✅ 直接导入 | 数据质量高 |
| 70-84 | 良好(Good) | ⚠️ 标记待审核 | 标记为pending_review |
| 50-69 | 一般(Fair) | 🔍 人工验证 | 进入审核队列 |
| < 50 | 较差(Poor) | ❌ 标记不可用 | 标记为invalid，不参与业务分析 |

#### 3.4.3 数据质量评分计算

⚠️ **推荐：采用统一的准确度评估機制**

参考《数据清洗业务类型完整分析_简化版本.md》中的通用准确度评估機制：

```python
# 准确度评估计算公式
field_accuracy_score = (
    data_source_reliability * 0.4     # 数据源权威性 (0-100)
    + logical_consistency * 0.3       # 业务业辑一致性 (0-100)
    + external_validation_match * 0.3 # 外部验证匹配度 (0-100)
)

# 准确度阈值与处理参数表
accuracy_thresholds = {
    'excellent': (85, 100),      # ✅ 自动采用
    'good': (70, 84),           # ⚠️ 标记待审查
    'fair': (50, 69),           # 🔍 人工验证
    'poor': (0, 49)             # ❌ 标记不可用
}
```

**SQL示例——带有准确度评估字段**：

```sql
-- 以电商商品为例，每个关键字段流洋有准确度评估字段
CREATE TABLE ec_goods_info (
    product_id VARCHAR(50) PRIMARY KEY,
    product_name VARCHAR(255) NOT NULL,
    category VARCHAR(50) NOT NULL,
    
    -- 业务字段
    price DECIMAL(10, 2),
    
    -- ❔ 准确度评估字段（每个关键字段）
    price_anal INT CHECK(price_anal >= 0 AND price_anal <= 100) COMMENT '价格准确度评分(0-100)',
    price_anal_info VARCHAR(500) COMMENT '价格准确度评估说明',
    
    product_name_anal INT CHECK(product_name_anal >= 0 AND product_name_anal <= 100),
    product_name_anal_info VARCHAR(500) COMMENT '商品名称准确度评估说明',
    
    category_anal INT CHECK(category_anal >= 0 AND category_anal <= 100),
    category_anal_info VARCHAR(500) COMMENT '分类准确度评估说明',
    
    -- 时间字段
    data_crawl_date DATE NOT NULL,
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    imported_at TIMESTAMP,
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE,
    quality_score INT CHECK(quality_score >= 0 AND quality_score <= 100),
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid',
    
    version INT DEFAULT 1,
    
    KEY idx_category (category),
    KEY idx_price_anal (price_anal),
    KEY idx_quality (quality_score)
);
```

**{field}_anal_info VARCHAR字段设计说明**：

`{field}_anal_info` 是VARCHAR(500)类型的文本字段，用于记录`{field}_anal`准确度评分的解释。字段内容为描述字符串，按照以下格式编写：

- 格式：`根据项1:XX分,根据项2:XX分,...,最终评分:XX分,信心度:{level},建议:{text}`
- 示例：`数据源权威性:95分(原始值),业务一致性:90分,外部验证匹配:88分,最终评分:91分,信心度:优秀,建议:直接使用`

**字段值的下执始例**：
```
price_anal = 91
price_anal_info = '数据源权威性:95分(原始值),业务一致性:90分,外部验证匹配:88分,最终评分:91分,信心度:优秀,建议:直接使用'

product_name_anal = 85
product_name_anal_info = '数据源权威性:90分(清洗后),业务一致性:85分,外部验证匹配:80分,最终评分:85分,信心度:优秀,建议:直接使用'
```

```python
# 准确度评估计算公式
field_accuracy_score = (
    data_source_reliability * 0.4     # 数据源权威性 (0-100)
    + logical_consistency * 0.3       # 业务逻辑一致性 (0-100)
    + external_validation_match * 0.3 # 外部验证匹配度 (0-100)
)

# 准确度阈值与处理参数表
accuracy_thresholds = {
    'excellent': (85, 100),      # ✅ 自动采用
    'good': (70, 84),           # ⚠️ 标记待审核
    'fair': (50, 69),           # 🔍 人工验证
    'poor': (0, 49)             # ❌ 标记不可用
}
```

**SQL示例——带有准确度评估字段**：

```sql
-- 以电商商品为例，槨上旨带有准确度计算字段
CREATE TABLE ec_goods_info (
    product_id VARCHAR(50) PRIMARY KEY,
    product_name VARCHAR(255) NOT NULL,
    category VARCHAR(50) NOT NULL,
    
    -- 业务字段
    price DECIMAL(10, 2),
    
    -- ❔ 准确度评估字段（为了每个关键字段）
    price_anal INT CHECK(price_anal >= 0 AND price_anal <= 100) COMMENT '价格准确度评分(0-100)',
    price_anal_info VARCHAR(500) COMMENT '价格准确度喷言描述',
    
    product_name_anal INT CHECK(product_name_anal >= 0 AND product_name_anal <= 100),
    product_name_anal_info VARCHAR(500) COMMENT '商品名称准确度评估说明',
    
    category_anal INT CHECK(category_anal >= 0 AND category_anal <= 100),
    category_anal_info VARCHAR(500) COMMENT '分类准确度评估说明',
    
    -- 时间字段
    data_crawl_date DATE NOT NULL,
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    imported_at TIMESTAMP,
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE,
    quality_score INT CHECK(quality_score >= 0 AND quality_score <= 100),
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid',
    
    version INT DEFAULT 1,
    
    KEY idx_category (category),
    KEY idx_price_anal (price_anal),
    KEY idx_quality (quality_score)
);

-- price_anal_info VARCHAR 例子
-- '数据源权威性:95分(原始值),业务一致性:90分,外部验证匹配:88分,最终评分:91分,信心度:优秀,建议:直接使用'
```

### 3.5 数据覆盖与版本管理

#### 3.5.1 数据覆盖策略

| 场景 | 覆盖规则 | 说明 |
| --- | --- | --- |
| **新增数据** | 直接插入 | 从未出现过的数据 |
| **重覆数据** | 检测并去重 | 主键相同的记录，保留最新版本 |
| **更新数据** | 按主键更新 | 主键相同但内容有变化，更新记录 |
| **冲突处理** | 以新数据为准 | 同一主键新旧版本冲突时，新覆盖旧 |

#### 3.5.2 冲突检测与处理

```python
# 数据冲突处理流程示例
def handle_data_insertion(new_record, primary_key_value, table_name):
    # 1. 检查是否存在相同主键的记录
    existing_record = db.find_by_primary_key(table_name, primary_key_value)
    
    if existing_record is None:
        # 新记录，直接插入
        db.insert(table_name, new_record)
        logger.info(f"新增记录: {primary_key_value}")
        
    elif existing_record == new_record:
        # 完全相同，跳过
        logger.debug(f"数据重覆，跳过: {primary_key_value}")
        
    else:
        # 记录存在但内容不同，更新记录
        db.update(table_name, primary_key_value, new_record)
        logger.info(f"更新记录: {primary_key_value}")
        record_version_change(primary_key_value, existing_record, new_record)
```

#### 3.5.3 版本管理方案

| 方案 | 实现方式 | 优势 | 劣势 |
| --- | --- | --- | --- |
| **直接覆盖** | 新数据覆盖旧数据，不保留历史 | 简单、存储空间小 | 无法追溯历史变化 |
| **版本字段** | 添加version字段标记数据版本 | 支持版本查询、轻量级 | 需適外维护版本号 |
| **历史表** | 维护一张历史表，记录所有变化 | 完整历史记录、审计需求满足 | 存储空间大、查询复杂 |
| **软删除** | 标记删除而非物理删除 | 支持恢复、完整历史 | 查询需过滤标记 |

**推荐方案**：使用version字段+last_updated_at时间戳的组合，简单高效。

### 3.6 业务类型特定的存储策略与示例



**推荐方案**：使用version字段+last_updated_at时间戳的组合，简单高效。

### 3.5 业务类型特定的存储策略

#### 3.6.1 电商数据存储策略

✅ **推荐：中规模策略（单表，所有商品类篱合）**

```sql
CREATE TABLE ec_goods_info (
    -- 统一商品表（电商所有商品分类）
    product_id VARCHAR(50) PRIMARY KEY,
    product_name VARCHAR(255) NOT NULL,
    
    -- 分类字段（主要区分维度）
    category VARCHAR(50) NOT NULL COMMENT '商品类挂 ：手机/电脑/鞋/衣服等',
    subcategory VARCHAR(100) COMMENT '步位类挂，如：iPhone/安卓',
    
    -- 业务字段
    platform VARCHAR(50) COMMENT '数据来源平台',
    price DECIMAL(10, 2),
    description LONGTEXT,
    
    -- 时间字段
    data_crawl_date DATE NOT NULL,
    data_update_time TIMESTAMP,
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    imported_at TIMESTAMP,
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE COMMENT '是否有效',
    quality_score INT CHECK(quality_score >= 0 AND quality_score <= 100) COMMENT '数据质量评分(0-100)',
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid' COMMENT '数据处理状态',
    validity_reason VARCHAR(500) COMMENT '无效原因',
    
    -- 版本控制
    version INT DEFAULT 1 COMMENT '数据版本号',
    
    -- 索引（重点：category字段方便按商品类型查询）
    KEY idx_category (category),
    KEY idx_subcategory (subcategory),
    KEY idx_platform (platform),
    KEY idx_crawl_date (data_crawl_date),
    KEY idx_status (data_status),
    KEY idx_quality (quality_score)
);
```

**存储特性**：
- 所有商品类正统一存储，数据量中等，性能高效
- 通过category字段查询特定类步的数据（例如所有手机、所有电脑）
- 便于扩展：步衤分类不需要创建新表，只需插入新记录
- 易于整体管理，单表的第一正位索引高效

**查询示例**：
```sql
-- 查询所有手机商品
SELECT * FROM ec_goods_info WHERE category = '手机';

-- 查询特定商品类步的优质数据
SELECT * FROM ec_goods_info WHERE category = '手机' AND quality_score >= 85;

-- 查询所有有效数据
SELECT * FROM ec_goods_info WHERE is_valid = true AND data_status = 'valid';
```

#### 3.5.2 社交评论数据存储策略

✅ **推荐：超大规模策略（按日期+分片数进行分片）**

```sql
-- 社交评论表 - 按日期+分片号分片
-- 示例：social_comment_20240101_00 （20240101日的第0片）
CREATE TABLE social_comment_20240101_00 (
    comment_id VARCHAR(100) PRIMARY KEY COMMENT '评论全局唯一ID',
    post_id VARCHAR(100) NOT NULL COMMENT '帖子ID',
    user_id VARCHAR(100) COMMENT '用户ID',
    
    -- 业务字段
    content LONGTEXT COMMENT '评论内容',
    likes_count INT DEFAULT 0 COMMENT '点赞数',
    reply_count INT DEFAULT 0 COMMENT '回割数',
    timestamp BIGINT COMMENT '原始时间戳',
    
    -- 时间字段
    data_crawl_date DATE NOT NULL COMMENT '数据抓取日期',
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '清洗完成时间',
    imported_at TIMESTAMP COMMENT '数据导入数据库时间',
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE,
    quality_score INT CHECK(quality_score >= 0 AND quality_score <= 100),
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid',
    
    -- 索引
    KEY idx_post_id (post_id),
    KEY idx_user_id (user_id),
    KEY idx_crawl_date (data_crawl_date),
    KEY idx_likes (likes_count)
) PARTITION BY RANGE (TO_DAYS(data_crawl_date)) (
    -- 每个日市一个分区，每天可以进一步分片（分片号 00/01/02）
    PARTITION p_default VALUES LESS THAN MAXVALUE
);
```

**存储特性**：
- 推荐分片数量：每日最多3-5片，根据实际数据量氛流需调整
- 表名规则：`social_comment_{YYYYMMDD}_{shard_id:02d}`
- 每个第表非常小（约数百万条），保证查询性能
- 支持並行写入和查询
- 过旧数据清理简单：直接DROP表

**分片选择策略**——hash活动字段：
```python
# 实现了与关业务字段活动收录（在一个分片表中）

# 方模一：窀流儿分片（不推荐）
# 窀流分载也会引伐表数据处理流、频第会变抗

# 方模二：情津作判辨分片（推荐）
import hashlib

def determine_shard_id(comment_id, total_shards=3):
    # 基于comment_id进行hash计算
    hash_value = hashlib.md5(comment_id.encode()).hexdigest()
    shard_id = int(hash_value, 16) % total_shards
    return f"{shard_id:02d}"

def determine_table_name(record, crawl_date):
    shard_id = determine_shard_id(record['comment_id'])
    table_name = f"social_comment_{crawl_date}_{shard_id}"
    return table_name  # 例如…social_comment_20240101_00

# 使用示例
for record in daily_comments:
    table = determine_table_name(record, '20240101')
    db.insert(table, record)  # 插入到对应分片表
```

**查询示例**：
```sql
-- 查询一整天的帖子评论
-- 扣复臭麅：需要同时查询3个分片表（UNION ALL）
SELECT * FROM social_comment_20240101_00 WHERE post_id = 'POST_123'
UNION ALL SELECT * FROM social_comment_20240101_01 WHERE post_id = 'POST_123'
UNION ALL SELECT * FROM social_comment_20240101_02 WHERE post_id = 'POST_123';

-- 查询特定用户的高质量评论
SELECT * FROM social_comment_20240101_00 WHERE user_id = 'USER_456' AND quality_score >= 85;
```

#### 3.5.3 资讯文章数据存储策略

✅ **推荐：中规模策略（单表）**

```sql
CREATE TABLE news_article_info (
    article_id VARCHAR(100) PRIMARY KEY COMMENT '文章主键',
    title VARCHAR(500) NOT NULL COMMENT '文章标题',
    content LONGTEXT COMMENT '文章正文',
    author VARCHAR(100) COMMENT '作者',
    source VARCHAR(100) COMMENT '来源平台',
    source_url VARCHAR(500) COMMENT '来源URL',
    
    -- 时间字段
    publish_date DATE COMMENT '预议发布日期',
    data_crawl_date DATE NOT NULL COMMENT '数据抓取日期',
    data_update_time TIMESTAMP COMMENT '文章更新时间',
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    imported_at TIMESTAMP,
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE,
    quality_score INT CHECK(quality_score >= 0 AND quality_score <= 100),
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid',
    
    -- 版本控制
    version INT DEFAULT 1,
    
    -- 索引
    KEY idx_publish_date (publish_date),
    KEY idx_source (source),
    KEY idx_crawl_date (data_crawl_date),
    KEY idx_status (data_status)
);
```

**存储特性**：
- 单表数据中等规模，性能性仸
- 支持帖子信息变更追踪（version字段）
- 支持发布日期、来源维度的聚合查询

---

#### 3.5.4 房产信息数据存储策略

✅ **推荐：中规模策略（单表或按年分片）**

> **为什么不是按日期分片？**
> 
> 房产信息本质是一个「字典类」数据（类似企业信息、车型信息），每个房产不过是数据库中的一条记录，数据是**更新**轴对事情部份是需要的，不是每天都有新房产轜为新记录添加。按日期分片的意义是扵汢接数据小量，但并不是这个场景的根本疑疑。

```sql
-- 房产信息表（单表或按年分片）
-- 一个房产就是数据库中的一条记录，仅根据业务需要拨会分片（按年分片即可）
-- 示例：proverty_info或property_info_2024
CREATE TABLE property_info (
    property_id VARCHAR(50) PRIMARY KEY COMMENT '房产全局唯一ID',
    property_name VARCHAR(255) NOT NULL COMMENT '房产名称/地址',
    city VARCHAR(50) COMMENT '城市',
    district VARCHAR(100) COMMENT '区县',
    price DECIMAL(15, 2) COMMENT '价格',
    area DECIMAL(10, 2) COMMENT '面积(平方米)',
    property_type VARCHAR(50) COMMENT '房产类型(住宅/商揺等)',
    
    -- 时间字段
    data_crawl_date DATE NOT NULL COMMENT '数据抓取日期',
    data_update_time TIMESTAMP COMMENT '房产更新时间',
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '清洗完成时间',
    imported_at TIMESTAMP,
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE COMMENT '数据是否有效',
    quality_score INT CHECK(quality_score >= 0 AND quality_score <= 100) COMMENT '数据质量评分',
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid' COMMENT '数据处理状态',
    
    -- 版本控制
    version INT DEFAULT 1 COMMENT '数据版本号',
    
    -- 索引
    KEY idx_city_district (city, district),
    KEY idx_price (price),
    KEY idx_property_type (property_type),
    KEY idx_crawl_date (data_crawl_date),
    KEY idx_status (data_status)
);
```

**为什么不是按日期分片、而是按年分片或单表：**

房产信息本质是一个「字典类」数据（类似企业信息、车型信息），每个房产就是数据库中的一条记录。这类数据是**更新**而不是**新增**，每天不是都有新房产添加，而是现有房产的价格、状态、配套设施等信息的更新。

按日期分片的设计初衷是为了**分散超高并发的写入**，例如社交平台每天有数百万条新评论不断地流入。但房产信息没有这个特点，其更新频率是相对较低的。

**推荐方案（一、二选一）：**

- 方案一：**单表存储** `property_info` - 适合住宅总数较少的场景
  - 优点：查询简单，无需JOIN，管理成本低
  - 应用：住宅总数<100万，每月更新频率中等

- 方案二：**按年分片** `property_info_2024 / property_info_2025 ...` - 按年度管理
  - 优点：按年隔离，便于旧数据清理和归档
  - 应用：住宅总数>100万，需要跨年度保存和管理

**存储特性**：
- 方案一：单表`property_info` - 适合住宅总数少、更新不频繁的场景
- 方案二：按年分片 - 适合住宅总数大、需要跨年管理的场景
- 每个房产是一条独立记录，通过版本字段追踪变更历史
- 支持按城市/区域/价格的多维度查询

**查询示例**：
```sql
-- 方案一：单表查询
SELECT * FROM property_info WHERE city = '上海' AND district = '浦东新区';

-- 方案二：按年分片查询
SELECT * FROM property_info_2024 WHERE city = '上海' AND price BETWEEN 1000000 AND 5000000;
```

---

#### 3.5.5 小规模业务类型（企业、用户、财务）

✅ **推荐：小规模策略（统一基础表）**

```sql
CREATE TABLE basic_entity_info (
    -- 唯一标识 + 类步
    entity_id VARCHAR(100) PRIMARY KEY COMMENT '实体全局唯一ID',
    entity_type VARCHAR(50) NOT NULL COMMENT '实体类型：enterprise/user/finance/...',
    
    -- 通用字段（JSON折叠）
    basic_info JSON NOT NULL COMMENT '结构化数据：{"name": "...", "industry": "..."}',
    extra_info JSON COMMENT '扩展字段',
    
    -- 时间字段
    data_crawl_date DATE NOT NULL COMMENT '数据抓取日期',
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    imported_at TIMESTAMP,
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    -- 有效性字段
    is_valid BOOLEAN DEFAULT TRUE,
    quality_score INT CHECK(quality_score >= 0 AND quality_score <= 100),
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid',
    
    -- 版本控制
    version INT DEFAULT 1,
    
    -- 索引
    KEY idx_entity_type (entity_type),
    KEY idx_crawl_date (data_crawl_date),
    KEY idx_status (data_status)
);
```

**存储特性**：
- 所有小规模数据类型合并，无需分片
- 统一管理，维护成本低
- 通过entity_type区分数据类型
- 使用JSON折叠字段灶载业务特定信息

**查询示例**：
```sql
-- 查询所有企业信息
SELECT * FROM basic_entity_info WHERE entity_type = 'enterprise';

-- 查询所有用户信息
SELECT * FROM basic_entity_info WHERE entity_type = 'user';

-- 查询有效的财务信息
SELECT * FROM basic_entity_info WHERE entity_type = 'finance' AND data_status = 'valid';
```

**存储特性**：
- 按enterprise_id作为唯一标识
- 企业信息相对稳定，采用版本管理策略
- 关键字段变化时触发版本更新

### 3.6 数据库表公共字段规范

#### 3.6.1 所有表必须包含的字段

**公共必需字段**：

| 字段名 | 字段类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| **业务主键** | VARCHAR/INT | PRIMARY KEY, NOT NULL | 根据业务类型定义，如product_id、article_id |
| **data_crawl_date** | DATE | NOT NULL | 原始数据抓取日期 |
| **last_updated_at** | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 最后更新时间 |
| **is_valid** | BOOLEAN | DEFAULT TRUE | 数据有效性标记 |
| **quality_score** | INT | CHECK(0-100), DEFAULT 100 | 数据质量评分 |
| **data_status** | ENUM | DEFAULT 'valid' | 数据处理状态 |
| **version** | INT | DEFAULT 1 | 数据版本号 |

**标签字段（必需，用于数据打标）**：

| 字段名 | 字段类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| **tag_confidence** | INT | CHECK(0-100), NOT NULL | 整体置信度(0-100)，代表这条记录所有标签的综合置信度；初步打标后设置，大模型优化后直接覆盖 |

> **标签字段设计说明**：
> - 业务表中**仅保留**`tag_confidence`（整体置信度），不存储具体标签值
> - **标签数据存储在独立标签表**（如`ec_comment_2024_tag`），每行一个标签
> - 标签表中包含：业务ID(FK)、标签值(tag_value)、标签置信度(tag_confidence)
> - 好处：支持成百上千的标签，索引高效，增删标签不需修改业务表
> - 初步打标：向标签表插入初始置信度的标签记录，更新业务表的tag_confidence为最高值
> - 大模型优化：删除旧标签或覆盖新置信度，重新计算业务表的tag_confidence

#### 3.6.2 推荐索引规范

| 索引类型 | 字段 | 说明 | 索引名示例 |
| --- | --- | --- | --- |
| **推荐索引规范** | 字段 | 说明 | 索引名示例 |
| --- | --- | --- | --- |
| **主键索引** | 业务主键 | 必须，保证唯一性 | PRIMARY KEY |
| **状态索引** | data_status | 支持数据状态筛选 | idx_data_status |
| **组合索引** | (last_updated_at, is_valid) | 支持常见查询模式 | idx_crawl_date_valid |
| **置信度索引** | tag_confidence | 支持整体置信度范围查询 | idx_tag_confidence |

#### 3.6.3 标签表设计（独立标签表方案）

业务表中仅保留整体置信度，具体标签存储在独立的标签表，支持大规模标签存储和高效查询。

**标签表命名规范**：`{business_table_name}_tag`

**示例**：业务表 `ec_comment_2024` 对应的标签表为 `ec_comment_2024_tag`

```sql
CREATE TABLE ec_comment_2024_tag (
    comment_id VARCHAR(100) NOT NULL COMMENT '关联的评论ID',
    tag_value VARCHAR(50) NOT NULL COMMENT '标签值（如：positive/urgent/complaint等）',
    tag_confidence INT NOT NULL CHECK(tag_confidence >= 0 AND tag_confidence <= 100) COMMENT '该标签的置信度(0-100)',
    
    -- 时间戳
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '标签创建时间',
    
    -- 主键和索引
    PRIMARY KEY (comment_id, tag_value),
    KEY idx_tag_value (tag_value),
    KEY idx_tag_confidence (tag_confidence),
    KEY idx_created_at (created_at),
    
    -- 外键约束（可选，根据业务需求）
    CONSTRAINT fk_comment_id FOREIGN KEY (comment_id) 
        REFERENCES ec_comment_2024(comment_id) ON DELETE CASCADE
) COMMENT='评论标签关联表';
```

**业务表修改**（仅保留整体置信度）：

```sql
CREATE TABLE ec_comment_2024 (
    comment_id VARCHAR(100) PRIMARY KEY COMMENT '评论主键',
    product_id VARCHAR(50) NOT NULL COMMENT '商品ID',
    user_id VARCHAR(100) COMMENT '用户ID',
    comment_text LONGTEXT COMMENT '评论内容',
    rating INT CHECK(rating >= 1 AND rating <= 5) COMMENT '评分(1-5)',
    
    -- 标签整体置信度（必需）
    tag_confidence INT NOT NULL CHECK(tag_confidence >= 0 AND tag_confidence <= 100) DEFAULT 0 COMMENT '整体置信度(0-100)，取所有标签置信度的最高值',
    
    -- 公共字段
    data_crawl_date DATE NOT NULL COMMENT '抓取日期',
    cleaned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '清洗完成时间',
    last_updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后更新时间',
    is_valid BOOLEAN DEFAULT TRUE COMMENT '数据是否有效',
    quality_score INT CHECK(quality_score >= 0 AND quality_score <= 100) COMMENT '数据质量评分',
    data_status ENUM('valid', 'pending_review', 'invalid') DEFAULT 'valid' COMMENT '数据处理状态',
    version INT DEFAULT 1 COMMENT '数据版本号',
    
    -- 索引
    KEY idx_product_id (product_id),
    KEY idx_user_id (user_id),
    KEY idx_tag_confidence (tag_confidence),
    KEY idx_data_status (data_status),
    KEY idx_last_updated (last_updated_at, is_valid)
) COMMENT='电商评论表';
```

#### 3.6.4 标签打标工作流程

**场景1：初步打标（规则匹配）**

```sql
-- 1. 插入初步打标结果到标签表
INSERT INTO ec_comment_2024_tag (comment_id, tag_value, tag_confidence)
VALUES 
    ('COMMENT_001', 'positive', 65),
    ('COMMENT_001', 'quality_review', 58);

-- 2. 更新业务表的整体置信度（取最高值）
UPDATE ec_comment_2024
SET tag_confidence = (
        SELECT MAX(tag_confidence) 
        FROM ec_comment_2024_tag 
        WHERE comment_id = 'COMMENT_001'
    ),
    last_updated_at = NOW()
WHERE comment_id = 'COMMENT_001';
```

**场景2：大模型优化（增加/替换标签）**

```sql
-- 1. 删除旧标签或更新置信度
DELETE FROM ec_comment_2024_tag 
WHERE comment_id = 'COMMENT_001' AND tag_value = 'quality_review';

-- 2. 插入大模型优化后的标签
INSERT INTO ec_comment_2024_tag (comment_id, tag_value, tag_confidence)
VALUES 
    ('COMMENT_001', 'positive', 92),
    ('COMMENT_001', 'urgent_complaint', 88);

-- 3. 更新业务表的整体置信度
UPDATE ec_comment_2024
SET tag_confidence = (
        SELECT MAX(tag_confidence) 
        FROM ec_comment_2024_tag 
        WHERE comment_id = 'COMMENT_001'
    ),
    last_updated_at = NOW()
WHERE comment_id = 'COMMENT_001';
```

**场景3：查询特定标签的记录（高效索引查询）**

```sql
-- 查询包含'urgent_complaint'标签的评论
SELECT c.comment_id, c.comment_text, c.rating, t.tag_value, t.tag_confidence
FROM ec_comment_2024 c
JOIN ec_comment_2024_tag t ON c.comment_id = t.comment_id
WHERE t.tag_value = 'urgent_complaint'
ORDER BY t.tag_confidence DESC;
```

**场景4：查询高置信度的记录**

```sql
-- 查询整体置信度 >= 90 的评论
SELECT c.comment_id, c.comment_text, c.tag_confidence,
       GROUP_CONCAT(CONCAT(t.tag_value, ':', t.tag_confidence)) as tags
FROM ec_comment_2024 c
LEFT JOIN ec_comment_2024_tag t ON c.comment_id = t.comment_id
WHERE c.tag_confidence >= 90
GROUP BY c.comment_id
ORDER BY c.tag_confidence DESC;
```

**场景5：标签分布统计**

```sql
-- 统计各标签的数量和平均置信度
SELECT tag_value, COUNT(*) as count, AVG(tag_confidence) as avg_confidence
FROM ec_comment_2024_tag
GROUP BY tag_value
ORDER BY count DESC;
```

### 3.7 数据异常处理规范

#### 3.7.1 异常分类与处理

| 异常类型 | 判断条件 | 处理方式 | 数据状态 |
| --- | --- | --- | --- |
| **字段缺失** | 必填字段为NULL | 标记为invalid | invalid |
| **格式错误** | 字段格式不符合规范 | 尝试修复，无法修复则标记 | invalid或pending_review |
| **业务逻辑错误** | 违反业务规则（如价格为负数） | 删除或标记为pending_review | invalid或pending_review |
| **重复数据** | 主键相同 | 保留最新版本，删除旧版本 | valid（保留版本） |
| **超出范围** | 数值超过预期范围 | 根据业务规则处理，可修复或标记 | invalid或valid（已修复） |

#### 3.7.2 异常处理流程

```python
def handle_data_exception(record, exception_type):
    """数据异常处理流程"""
    
    if exception_type == 'missing_required_field':
        # 必填字段缺失 → 标记为无效
        record['is_valid'] = False
        record['data_status'] = 'invalid'
        record['validity_reason'] = f"缺少必填字段"
        record['quality_score'] = calculate_quality_score(record)
        
    elif exception_type == 'format_error':
        # 格式错误 → 尝试修复
        try:
            record = fix_format_error(record)
            record['is_valid'] = True
            record['data_status'] = 'valid'
            record['quality_score'] = calculate_quality_score(record)
        except:
            record['is_valid'] = False
            record['data_status'] = 'invalid'
            record['validity_reason'] = "格式错误无法修复"
            
    elif exception_type == 'business_logic_error':
        # 业务逻辑错误 → 标记待审核
        record['is_valid'] = False
        record['data_status'] = 'pending_review'
        record['validity_reason'] = "违反业务规则，需人工审核"
        record['quality_score'] = calculate_quality_score(record)
        
    return record
```

---

## 第四部分：数据一致性与校验

### 4.1 数据一致性校验

#### 4.1.1 校验层级

| 校验层级 | 触发时机 | 校验内容 | 处理方式 |
| --- | --- | --- | --- |
| **元数据校验** | 清洗前 | 元数据文件完整性、格式正确性 | 拒绝处理，记录错误 |
| **数据类型校验** | 清洗中 | 字段类型是否符合预期 | 尝试转换，失败则标记 |
| **业务规则校验** | 清洗中 | 数据是否符合业务逻辑 | 根据规则修复或标记 |
| **数据库校验** | 导入前 | 数据是否满足表结构要求 | 拒绝导入，记录错误 |
| **完整性校验** | 导入后 | 数据是否完整导入 | 对比导入前后的记录数 |

#### 4.1.2 校验检查清单

```
清洗前校验：
☐ 元数据文件存在
☐ 元数据格式有效（valid JSON）
☐ 元数据必填字段完整
☐ 业务类型ID在支持列表中

清洗中校验：
☐ 数据文件可读性
☐ 数据格式与元数据匹配
☐ 字段类型转换成功
☐ 业务规则检查通过

清洗后校验：
☐ 清洗记录数与预期相符
☐ 所有必填字段非空
☐ 时间戳字段有效
☐ 数据质量评分完成

导入前校验：
☐ 表结构完整
☐ 主键字段有值
☐ 无SQL注入风险
☐ 字符编码正确

导入后校验：
☐ 实际导入记录数 = 预期导入数
☐ 主键唯一性检查通过
☐ 索引构建成功
☐ 数据可查询
```

---

## 第五部分：数据治理与维护

### 5.1 数据质量监控

#### 5.1.1 关键指标

| 指标 | 计算方式 | 目标值 | 告警阈值 |
| --- | --- | --- | --- |
| **数据完整性** | (非空字段数/总字段数) × 100% | ≥ 95% | < 90% |
| **数据准确性** | 根据业务规则校验 | ≥ 90% | < 85% |
| **数据有效率** | (有效记录数/总记录数) × 100% | ≥ 85% | < 80% |
| **去重率** | (删除记录数/总记录数) × 100% | 5-15% | > 20% |
| **清洗成功率** | (成功清洗数/提交清洗数) × 100% | ≥ 98% | < 95% |

#### 5.1.2 监控告警

```python
def quality_alert(metric_name, metric_value, threshold):
    """质量指标告警"""
    
    if metric_value < threshold:
        alert_level = 'CRITICAL' if metric_value < threshold * 0.8 else 'WARNING'
        alert_message = f"{alert_level}: {metric_name} = {metric_value}%, 阈值 = {threshold}%"
        send_alert(alert_message)
        log_warning(metric_name, metric_value, threshold)
```

### 5.2 数据清理与维护

#### 5.2.1 定期清理计划

| 清理对象 | 清理周期 | 清理条件 | 保留策略 |
| --- | --- | --- | --- |
| **临时数据文件** | 每日执行 | 处理完成7天后 | 成功数据归档，失败数据保留30天 |
| **已处理标记** | 每周执行 | 清理过期的处理记录 | 仅保留最近30天的日志 |
| **无效数据** | 每月执行 | is_valid=false且30天未更新 | 根据业务需求转移至历史库 |
| **日志文件** | 每周执行 | 日志文件超过500MB或存储30天 | 按日期分片压缩保存 |

#### 5.2.2 数据备份策略

| 备份类型 | 备份频率 | 保留时间 | 说明 |
| --- | --- | --- | --- |
| **完整备份** | 每周日23:00 | 保留2个月 | 全量数据库备份 |
| **增量备份** | 每天23:00 | 保留7天 | 仅备份新增/修改数据 |
| **临时数据备份** | 处理时实时 | 7天 | 清洗前后数据对比备份 |

---

## 附录：快速参考

### A. 文件命名速查表

```
EC_GOODS_PHONE_20240108_0001.json          # 电商商品
EC_GOODS_PHONE_20240108_0001.metadata.json # 对应元数据

SOCIAL_COMMENT_20240108_0001.jsonl         # 社交评论
SOCIAL_COMMENT_20240108_0001.metadata.json # 对应元数据

NEWS_ARTICLE_20240108_0001.jsonl           # 资讯文章
NEWS_ARTICLE_20240108_0001.metadata.json   # 对应元数据
```

### B. 业务类型与表名映射速查表

| 业务类型ID | 数据库表名 | 主键字段示例 |
| --- | --- | --- |
| EC_GOODS_PHONE | ec_goods_info | product_id |
| EC_COMMENT | ec_comment_YYYYMM | comment_id |
| SOCIAL_COMMENT | social_comment_YYYY_XX | comment_id |
| SOCIAL_POST | social_post_YYYY_XX | post_id |
| NEWS_ARTICLE | news_article_info | article_id |
| FINANCE_REPORT | financial_report | (company_code, report_period, publish_date) |
| ENTERPRISE_INFO | company_info | company_code |
| RECRUIT_JOB | recruit_job_info | job_id |
| VIDEO_METADATA | video_metadata_info | video_id |
| USER_PROFILE | user_profile | user_id |
| PROPERTY_INFO | property_info_YYYYMMDD | property_id |
| CAR_INFO | car_info | car_id |
| PURCHASE_ORDER | purchase_order_YYYYMM | order_id |

### C. 常见问题解答

**Q: 元数据文件丢失怎么办？**
A: 清洗MCP会自动检测并报错，需要上游重新提交带元数据的数据文件。

**Q: 同一业务类型同一天有多个文件怎么处理？**
A: 使用序号区分，如0001、0002、0003等，清洗MCP会逐个处理。

**Q: 清洗失败的数据如何处理？**
A: 失败数据被移至failed目录，保留30天供问题排查。

**Q: 如何查询清洗后的数据？**
A: 由专端提供QueryMCP或数据库查询服务，工作流向数据库查询而不是从清洗MCP获取。

**Q: 数据覆盖策略是什么？**
A: 按业务主键检测重复，新数据覆盖旧版本，version字段记录版本号。

---

## 文档维护

| 版本 | 更新日期 | 更新内容 | 维护者 |
| --- | --- | --- | --- |
| v1.0 | 2024-01-08 | 初版发布 | 数据清洗团队 |

**文档更新流程**：
1. 新增业务类型时，更新第2.3节业务类型定义
2. 修改存储策略时，更新第3.5节特定存储策略
3. 重大变更需通过团队评审后更新文档
4. 每季度对文档进行完整性检查

