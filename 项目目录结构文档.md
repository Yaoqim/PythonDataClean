# Python MCP 数据清洗服务 - 项目目录结构设计文档

## 文档说明

本文档设计了符合项目规范的完整目录结构。设计基于以下核心要求：
- 基于《项目规范.md》的三核心功能范围
- 基于《文字质量保障体系.md》的编码规范
- 基于《数据存储规范.md》的数据管理策略
- 基于《数据清洗业务类型完整分析_简化版本.md》的业务类型分类

---

## 完整目录树结构

```
PythonDataClean/
│
├── config/                           # [数据库配置模块] 数据库连接配置、访问接口、连接池管理
│   ├── __init__.py
│   ├── database_config.py            # 数据库连接配置（支持MySQL、PostgreSQL、SQLite）
│   ├── connection_pool.py            # 连接池管理，支持多数据库并发访问
│   ├── db_credentials.py             # 数据库凭证管理（环境变量或加密配置）
│   ├── settings.py                   # 全局配置参数（超时、batch大小等）
│   │
│   └── metadata_rules/               # 元数据规则配置
│       ├── __init__.py
│       ├── default_rule.json         # 通用清洗规则基础模板
│       ├── ec_goods_rule.json        # 电商商品清洗规则
│       ├── ec_comment_rule.json      # 电商评论清洗规则
│       ├── social_comment_rule.json  # 社交评论清洗规则
│       ├── social_post_rule.json     # 社交帖子清洗规则
│       ├── news_article_rule.json    # 资讯文章清洗规则
│       ├── finance_report_rule.json  # 财务报表清洗规则
│       ├── enterprise_info_rule.json # 企业工商信息清洗规则
│       ├── recruit_job_rule.json     # 招聘岗位清洗规则
│       ├── video_metadata_rule.json  # 短视频元数据清洗规则
│       ├── user_profile_rule.json    # 用户基础信息清洗规则
│       ├── property_info_rule.json   # 房产信息清洗规则
│       ├── car_info_rule.json        # 汽车信息清洗规则
│       ├── purchase_order_rule.json  # 采购订单清洗规则
│       └── customer_dialog_rule.json # 客户对话清洗规则
│
├── src/                              # [源代码主目录] 项目核心模块
│   ├── __init__.py
│   │
│   ├── file_service/                 # [文件操作模块] 阿里云服务器文件获取、读写、管理
│   │   ├── __init__.py
│   │   ├── file_handler.py           # 文件读写基础操作（支持JSON、CSV、JSONL、Parquet等）
│   │   ├── aliyun_client.py          # 阿里云服务器客户端集成
│   │   ├── file_validator.py         # 文件验证（编码、格式、完整性）
│   │   ├── metadata_loader.py        # 元数据文件加载器
│   │   └── file_utils.py             # 文件工具函数（路径处理、命名规范检查等）
│   │
│   ├── db_service/                   # [数据库配置模块] 数据库连接、访问接口、连接池管理
│   │   ├── __init__.py
│   │   ├── db_manager.py             # 数据库管理器（连接、事务、资源释放）
│   │   ├── db_handler.py             # 数据库操作处理器（增删改查）
│   │   ├── connection_pool.py        # 连接池实现
│   │   ├── table_manager.py          # 数据表创建与管理（按分层规则自动创建表结构）
│   │   ├── query_builder.py          # SQL查询构建器（支持分片查询等）
│   │   └── db_utils.py               # 数据库工具函数
│   │
│   ├── ai_service/                   # [AI模型调用模块] 大模型API请求、调用接口、响应处理
│   │   ├── __init__.py
│   │   ├── api_client.py             # 大模型API客户端（支持多种模型）
│   │   ├── prompt_builder.py         # 提示词构建器
│   │   ├── response_parser.py        # API响应解析处理
│   │   ├── model_config.py           # 模型配置参数
│   │   ├── retry_handler.py          # 重试机制与超时处理
│   │   └── cache_manager.py          # 缓存管理（避免重复API调用）
│   │
│   ├── cleaner/                      # [业务清洗服务模块] 核心数据清洗功能
│   │   ├── __init__.py
│   │   ├── base_cleaner.py           # 清洗器基类（定义通用清洗接口）
│   │   │
│   │   ├── cleaners/                 # 按业务类型的具体清洗实现
│   │   │   ├── __init__.py
│   │   │   ├── ec_goods_cleaner.py              # 电商商品清洗器
│   │   │   ├── ec_comment_cleaner.py           # 电商评论清洗器
│   │   │   ├── social_comment_cleaner.py       # 社交评论清洗器
│   │   │   ├── social_post_cleaner.py          # 社交帖子清洗器
│   │   │   ├── news_article_cleaner.py         # 资讯文章清洗器
│   │   │   ├── finance_report_cleaner.py       # 财务报表清洗器
│   │   │   ├── enterprise_info_cleaner.py      # 企业工商信息清洗器
│   │   │   ├── recruit_job_cleaner.py          # 招聘岗位清洗器
│   │   │   ├── video_metadata_cleaner.py       # 短视频元数据清洗器
│   │   │   ├── user_profile_cleaner.py         # 用户基础信息清洗器
│   │   │   ├── property_info_cleaner.py        # 房产信息清洗器
│   │   │   ├── car_info_cleaner.py             # 汽车信息清洗器
│   │   │   ├── purchase_order_cleaner.py       # 采购订单清洗器
│   │   │   └── customer_dialog_cleaner.py      # 客户对话清洗器
│   │   │
│   │   ├── strategies/               # 清洗策略与算法
│   │   │   ├── __init__.py
│   │   │   ├── deduplication.py      # 去重处理器（按唯一标识去重）
│   │   │   ├── normalization.py      # 字段标准化处理（文本、数值、时间、分类等）
│   │   │   ├── missing_handler.py    # 缺失值处理（删除/填充/标注）
│   │   │   ├── anomaly_detector.py   # 异常值检测与修正
│   │   │   ├── quality_scorer.py     # 数据质量评分器（准确度计算）
│   │   │   └── batch_processor.py    # 批处理器（支持不同数据量级的优化策略）
│   │   │
│   │   └── cleaner_factory.py        # 清洗器工厂类（根据业务类型创建对应清洗器）
│   │
│   ├── metadata/                     # [元数据处理模块] 元数据加载、验证、缓存
│   │   ├── __init__.py
│   │   ├── metadata_loader.py        # 元数据文件加载器
│   │   ├── metadata_validator.py     # 元数据验证（必填字段、格式、有效性）
│   │   ├── metadata_cache.py         # 元数据缓存管理
│   │   ├── business_type_registry.py # 业务类型注册表
│   │   └── metadata_utils.py         # 元数据工具函数
│   │
│   ├── storage/                      # [数据存储模块] 数据存储、表管理、分片策略
│   │   ├── __init__.py
│   │   ├── db_handler.py             # 数据库操作处理
│   │   ├── storage_strategy.py       # 存储策略（小规模、中规模、大规模、超大规模）
│   │   ├── sharding_manager.py       # 分片管理器（自动判断目标表并执行分片写入）
│   │   ├── table_schema_manager.py   # 表结构管理（按分层规则创建/更新表）
│   │   └── storage_utils.py          # 存储工具函数
│   │
│   ├── utils/                        # [工具函数模块] 日志、异常处理、通用工具
│   │   ├── __init__.py
│   │   ├── logger.py                 # 日志管理器（支持多级别、滚动日志）
│   │   ├── error_handler.py          # 异常处理与错误恢复
│   │   ├── time_utils.py             # 时间处理工具
│   │   ├── string_utils.py           # 字符串处理工具（编码规范化等）
│   │   ├── validation.py             # 验证工具（格式、范围、一致性）
│   │   ├── encoding_checker.py       # 编码检查器（确保UTF-8，预防乱码）
│   │   └── statistics.py             # 统计工具（去重率、缺失率等）
│   │
│   └── orchestrator.py               # 清洗流程编排器（协调各模块执行）
│
├── data/                             # [数据文件目录] 临时数据与处理结果
│   ├── temp/                         # 临时数据存储（处理中的文件）
│   │   └── .gitkeep
│   ├── processed/                    # 已处理数据记录（处理完成的文件日志）
│   │   └── .gitkeep
│   └── failed/                       # 清洗失败数据存档
│       └── .gitkeep
│
├── logs/                             # [日志文件目录] 系统日志、处理日志、错误日志
│   ├── .gitkeep
│   └── README.md                     # 日志管理说明
│
├── tests/                            # [测试文件目录] 单元测试、集成测试、性能测试
│   ├── __init__.py
│   ├── conftest.py                   # pytest配置与fixtures
│   │
│   ├── unit_tests/                   # 单元测试
│   │   ├── __init__.py
│   │   ├── test_file_service.py      # 文件服务模块测试
│   │   ├── test_db_service.py        # 数据库服务模块测试
│   │   ├── test_metadata.py          # 元数据模块测试
│   │   ├── test_cleaner_strategies.py # 清洗策略测试
│   │   ├── test_cleaners/
│   │   │   ├── __init__.py
│   │   │   ├── test_ec_goods_cleaner.py
│   │   │   ├── test_social_comment_cleaner.py
│   │   │   └── ...
│   │   └── test_utils.py             # 工具函数测试
│   │
│   ├── integration_tests/            # 集成测试
│   │   ├── __init__.py
│   │   ├── test_full_pipeline.py     # 完整清洗流程集成测试
│   │   └── test_business_types.py    # 按业务类型的集成测试
│   │
│   └── fixtures/                     # 测试数据与fixture
│       ├── sample_data/
│       │   ├── ec_goods_sample.json
│       │   ├── social_comment_sample.jsonl
│       │   └── ...
│       └── metadata_fixtures/
│           ├── metadata_sample.json
│           └── ...
│
├── main.py                           # 项目主程序入口
├── requirements.txt                  # Python依赖包清单
├── setup.py                          # 项目安装配置
│
├── 项目规范.md                       # [参考] 项目总体规范
├── 文字质量保障体系.md               # [参考] 编码与乱码防控规范
├── 数据存储规范.md                   # [参考] 数据存储与表设计规范
├── 清洗前置指南.md                   # [参考] 清洗前置步骤说明
│
├── bussesType/                       # 业务类型文档库
│   ├── 业务类型_1电商平台数据.md
│   ├── 业务类型_2电商商品评论.md
│   ├── 业务类型_3社交平台评论.md
│   ├── 业务类型_4社交平台帖子.md
│   ├── 业务类型_5资讯文章.md
│   ├── 业务类型_6财务报表.md
│   ├── 业务类型_7企业工商信息.md
│   ├── 业务类型_8招聘岗位信息.md
│   ├── 业务类型_9短视频元数据.md
│   ├── 业务类型_10用户基础信息.md
│   ├── 业务类型_11房产信息.md
│   ├── 业务类型_12汽车信息.md
│   ├── 业务类型_13采购订单信息.md
│   ├── 业务类型_14客户对话.md
│   └── 数据清洗业务类型完整分析_简化版本.md
│
└── README.md                         # 项目总体说明
```

---

## 模块职责详细说明

### 1. 文件操作模块 (src/file_service/)

**核心职责**：从阿里云服务器获取、读写和管理临时数据文件

**主要类与方法**：
- `FileHandler`：文件读写基础操作
  - `read_data_file(path)`：读取数据文件（JSON、CSV、JSONL、Parquet）
  - `write_data_file(path, data, format)`：写入数据文件
  - `validate_file(path)`：验证文件编码、格式、完整性
  
- `AliyunClient`：阿里云服务器集成
  - `connect()`：建立连接
  - `list_files(prefix)`：列出指定路径的文件
  - `download_file(remote_path, local_path)`：下载文件
  - `upload_file(local_path, remote_path)`：上传文件
  
- `MetadataLoader`：元数据加载
  - `load_metadata(file_path)`：加载同名的.metadata.json文件
  - `extract_metadata_info(metadata)`：提取关键元数据字段
  
- `FileValidator`：文件验证
  - `check_encoding(file_path)`：检查UTF-8编码（预防乱码）
  - `validate_format(file_path, expected_format)`：格式验证
  - `verify_checksum(file_path, checksum)`：完整性校验

**关键特性**：
- 支持多种文件格式（JSON、CSV、JSONL、Parquet等）
- UTF-8编码强制检查与转换
- 元数据自动匹配与加载
- 文件完整性验证

---

### 2. 数据库配置模块 (src/db_service/)

**核心职责**：数据库连接配置、连接池管理、表结构管理

**主要类与方法**：
- `DatabaseManager`：数据库管理器
  - `connect(db_type, credentials)`：建立数据库连接
  - `get_connection()`：从连接池获取连接
  - `close_connection(conn)`：释放连接
  - `execute_transaction(operations)`：执行事务
  
- `TableSchemaManager`：表结构管理
  - `create_table(business_type, data_size_level)`：按分层规则创建表
  - `get_target_table_name(business_type, record_key, crawl_date)`：确定目标表名（支持分片）
  - `ensure_table_exists(table_name)`：表存在性检查
  
- `ShardingManager`：分片管理
  - `determine_shard_key(record, business_type)`：计算分片键
  - `get_shard_table_name(base_name, shard_id, year)`：生成分片表名
  - `distribute_records(records, business_type)`：分散记录到不同分片
  
- `QueryBuilder`：SQL查询构建
  - `build_insert_query(table_name, fields, values)`：构建INSERT语句
  - `build_update_query(table_name, conditions, values)`：构建UPDATE语句
  - `build_sharded_query(business_type, filters)`：构建跨分片查询

**存储分层策略**（自动判断）：
- 小规模 (<10万/年)：统一基础表
- 中规模 (10-100万/年)：业务类型独立表
- 大规模 (100-1000万/年)：按时间分片表（月分片）
- 超大规模 (>1000万/年)：按年份+分片号分片

**关键特性**：
- 支持MySQL、PostgreSQL、SQLite多数据库
- 连接池优化并发访问
- 自动表结构创建与管理
- 智能分片与路由

---

### 3. AI模型调用模块 (src/ai_service/)

**核心职责**：大模型API请求、调用接口、响应处理

**主要类与方法**：
- `APIClient`：API调用客户端
  - `call_model(prompt, model_id, params)`：调用大模型API
  - `batch_call(prompts, model_id)`：批量调用
  - `stream_call(prompt, model_id)`：流式调用
  
- `PromptBuilder`：提示词构建
  - `build_cleaning_prompt(business_type, sample_data)`：构建清洗指导提示词
  - `build_quality_eval_prompt(field, value)`：构建质量评估提示词
  
- `ResponseParser`：响应解析
  - `parse_response(response_text)`：解析API响应JSON
  - `extract_field_value(response)`：提取清洗后的字段值
  - `extract_quality_score(response)`：提取质量评分
  
- `RetryHandler`：重试与超时
  - `retry_with_backoff(func, max_retries)`：指数退避重试
  - `handle_timeout(timeout_seconds)`：超时处理
  
- `CacheManager`：缓存管理
  - `get_cached_result(prompt_hash)`：获取缓存结果
  - `cache_result(prompt_hash, result, ttl)`：缓存API结果

**关键特性**：
- 支持多种大模型接口
- 自动重试与超时处理
- 响应结果缓存（避免重复调用）
- 支持批量与流式调用

---

### 4. 业务清洗服务模块 (src/cleaner/)

**核心职责**：针对不同业务类型的数据清洗

**主要类与方法**：

**清洗器基类 (BaseCleaner)**：
```python
class BaseCleaner:
    def clean(self, data, metadata) -> CleaningResult:
        """执行完整清洗流程"""
        # 1. 明确清洗目标
        # 2. 去重处理
        # 3. 字段标准化
        # 4. 缺失值处理
        # 5. 异常值修正
        pass
    
    def get_clean_rules(self) -> Dict:
        """获取业务类型的清洗规则"""
        pass
    
    def calculate_quality_score(self, record) -> int:
        """计算数据质量评分"""
        pass
```

**具体清洗器（如ec_goods_cleaner.py）**：
- `EcGoodsCleaner`：电商商品清洗
  - 字段：product_id、product_name、category、price等
  - 特殊处理：价格单位转换、分类规范化
  
- `SocialCommentCleaner`：社交评论清洗
  - 字段：comment_id、user_id、content、likes等
  - 特殊处理：文本规范化、情感标签提取、垃圾内容过滤

**清洗策略类 (src/cleaner/strategies/)**：
- `Deduplication`：去重处理
  - `deduplicate_by_id(records, id_field)`：按ID去重
  - `deduplicate_complete(records)`：完全重复检测
  
- `Normalization`：字段标准化
  - `normalize_text(value)`：文本规范化（去空格、特殊字符）
  - `normalize_number(value, unit=None)`：数值规范化（单位转换）
  - `normalize_time(value)`：时间规范化（格式统一）
  - `normalize_category(value)`：分类规范化（标签归一化）
  
- `MissingHandler`：缺失值处理
  - `handle_missing_values(records, rules)`：缺失值处理
  - `calculate_missing_rate(records)`：缺失率计算
  
- `AnomalyDetector`：异常检测
  - `detect_numeric_anomaly(value, range)`：数值异常检测
  - `detect_format_anomaly(value, pattern)`：格式异常检测
  - `repair_or_mark(value, strategy)`：修复或标记
  
- `QualityScorer`：质量评分
  - `calculate_field_accuracy(field_name, value, source)`：字段准确度评分
  - `calculate_overall_quality(record)`：整体质量评分
  
- `BatchProcessor`：批处理
  - `process_batch(records, cleaner, batch_size)`：智能批处理
  - `optimize_batch_size(data_size, data_level)`：优化batch大小

**清洗器工厂 (CleanerFactory)**：
```python
class CleanerFactory:
    @staticmethod
    def create_cleaner(business_type_id: str) -> BaseCleaner:
        """根据业务类型ID创建对应清洗器"""
        # 返回对应的cleaner实例
```

**关键特性**：
- 13种核心业务类型专用清洗器
- 统一的5步清洗流程框架
- 字段级准确度评估
- 支持不同数据规模的优化策略

---

### 5. 元数据处理模块 (src/metadata/)

**核心职责**：元数据加载、验证、缓存管理

**主要类与方法**：
- `MetadataLoader`：元数据加载
  - `load(file_path)`：加载.metadata.json文件
  - `auto_find_metadata(data_file_path)`：自动查找同名元数据文件
  
- `MetadataValidator`：元数据验证
  - `validate(metadata)`：完整验证
  - `validate_required_fields(metadata)`：必填字段检查
  - `validate_field_values(metadata)`：字段值有效性检查
  
- `MetadataCache`：元数据缓存
  - `get(business_type_id)`：获取缓存元数据
  - `set(business_type_id, metadata)`：缓存元数据
  - `clear_expired()`：清理过期缓存
  
- `BusinessTypeRegistry`：业务类型注册表
  - `register(business_type_id, config)`：注册业务类型
  - `get_config(business_type_id)`：获取业务类型配置

**关键特性**：
- 必填字段自动验证（8个必填字段）
- 业务类型ID注册管理
- 元数据缓存优化性能
- 支持UTF-8编码检查

---

### 6. 数据存储模块 (src/storage/)

**核心职责**：清洗后数据的存储、表管理、分片策略执行

**主要类与方法**：
- `StorageStrategy`：存储策略选择
  - `get_strategy(business_type, data_level)`：选择存储策略
  - `apply_strategy(records, strategy)`：应用存储策略
  
- `ShardingManager`：分片管理
  - `get_target_table(record, business_type)`：获取目标表
  - `distribute_to_shards(records, business_type)`：分散到分片
  - `build_shard_insert_sql(table_name, records)`：构建分片INSERT
  
- `TableSchemaManager`：表结构管理
  - `create_table_schema(business_type, data_level)`：创建表结构
  - `add_accuracy_fields(table_name)`：添加准确度评估字段
  - `ensure_indexes(table_name, fields)`：确保索引存在
  
- `StorageUtils`：存储工具
  - `batch_insert(table_name, records, batch_size)`：批量插入
  - `handle_duplicates(table_name, records, primary_key)`：处理重复数据
  - `add_timestamp_fields(records)`：添加时间戳字段

**表命名规范**（自动执行）：
```
小规模: basic_entity_info
中规模: {business_type}_info
大规模: {business_type}_YYYYMM
超大规模: {business_type}_YYYY_XX (XX为分片号)
```

**关键特性**：
- 4层存储分层策略自动判断
- 分片表自动创建与路由
- 准确度评估字段自动添加
- 时间戳字段自动管理

---

### 7. 工具函数模块 (src/utils/)

**核心职责**：日志、异常处理、编码检查、通用工具

**主要工具类**：
- `Logger`：日志管理
  - `debug()`、`info()`、`warning()`、`error()`
  - 自动日志滚动、多级别输出
  
- `ErrorHandler`：异常处理
  - `handle_file_read_error()`：文件读取异常处理
  - `handle_db_error()`：数据库异常处理
  - `handle_api_error()`：API调用异常处理
  
- `EncodingChecker`：编码检查（预防乱码）
  - `check_utf8(file_path)`：UTF-8编码检查
  - `convert_to_utf8(data)`：转换为UTF-8
  - `detect_and_fix_encoding(text)`：检测并修复编码
  
- `TimeUtils`：时间处理
  - `standardize_timestamp(value, format)`：时间戳规范化
  - `convert_relative_to_absolute()`：相对时间转绝对时间
  
- `StringUtils`：字符串处理
  - `remove_whitespace(text)`：去空格
  - `remove_special_chars(text)`：去特殊字符
  - `normalize_case(text)`：大小写规范化
  
- `Validation`：通用验证
  - `validate_range(value, min, max)`：范围检查
  - `validate_pattern(value, pattern)`：正则检查
  - `validate_consistency()`：一致性检查
  
- `Statistics`：统计工具
  - `calculate_dedup_rate()`：去重率计算
  - `calculate_missing_rate()`：缺失率计算
  - `generate_cleaning_report()`：生成清洗报告

**关键特性**：
- 完整的日志系统（30天自动清理）
- UTF-8编码强制检查与修复
- 详细的异常处理与错误恢复
- 清洗统计与质量报告

---

### 8. 流程编排模块 (src/orchestrator.py)

**核心职责**：协调各模块执行清洗流程

**主要流程**：
```python
def execute_cleaning_pipeline(file_paths: List[str]):
    """完整清洗流程编排"""
    
    # 1. 文件获取与验证
    for file_path in file_paths:
        # 从阿里云获取文件
        # 验证文件格式与编码
        pass
    
    # 2. 元数据加载与验证
    for file_path in file_paths:
        # 加载.metadata.json
        # 验证元数据字段
        # 获取业务类型与清洗规则
        pass
    
    # 3. 数据清洗
    for file_path, metadata in zip(file_paths, metadata_list):
        # 创建对应cleaner
        # 执行5步清洗流程
        # 计算质量评分
        pass
    
    # 4. 结果存储
    for cleaned_data, metadata in zip(cleaned_list, metadata_list):
        # 确定目标表（支持分片）
        # 批量插入到数据库
        # 记录处理统计
        pass
    
    # 5. 生成报告
    # 汇总清洗统计信息
    # 记录日志
    # 返回处理结果
```

**关键特性**：
- 统一的5步清洗框架
- 自动异常处理与恢复
- 完整的处理日志记录
- 详细的清洗统计报告

---

## 模块间通信与依赖关系

### 调用链路图

```
main.py (主程序入口)
  ↓
Orchestrator (流程编排器)
  ├─→ FileService (文件操作)
  │    ├─→ AliyunClient (阿里云客户端)
  │    ├─→ FileHandler (文件读写)
  │    ├─→ FileValidator (文件验证)
  │    └─→ MetadataLoader (元数据加载)
  │
  ├─→ Metadata (元数据处理)
  │    ├─→ MetadataLoader (加载)
  │    ├─→ MetadataValidator (验证)
  │    ├─→ BusinessTypeRegistry (注册表)
  │    └─→ MetadataCache (缓存)
  │
  ├─→ Cleaner (数据清洗)
  │    ├─→ CleanerFactory (创建cleaner)
  │    ├─→ BusinessTypeCleaners (各业务类型cleaner)
  │    └─→ CleaningStrategies (清洗策略)
  │         ├─→ Deduplication (去重)
  │         ├─→ Normalization (规范化)
  │         ├─→ MissingHandler (缺失处理)
  │         ├─→ AnomalyDetector (异常检测)
  │         └─→ QualityScorer (质量评分)
  │
  ├─→ AI_Service (AI模型调用) [可选]
  │    ├─→ APIClient (API调用)
  │    ├─→ PromptBuilder (提示构建)
  │    ├─→ ResponseParser (响应解析)
  │    └─→ CacheManager (结果缓存)
  │
  ├─→ DBService (数据库服务)
  │    ├─→ DatabaseManager (连接管理)
  │    ├─→ ConnectionPool (连接池)
  │    ├─→ TableSchemaManager (表管理)
  │    └─→ QueryBuilder (SQL构建)
  │
  ├─→ Storage (数据存储)
  │    ├─→ StorageStrategy (存储策略)
  │    ├─→ ShardingManager (分片管理)
  │    └─→ TableSchemaManager (表创建)
  │
  └─→ Utils (工具函数)
       ├─→ Logger (日志)
       ├─→ ErrorHandler (异常处理)
       ├─→ EncodingChecker (编码检查)
       ├─→ Statistics (统计)
       └─→ Validation (验证)
```

### 模块通信接口

| 发送方 | 接收方 | 数据类型 | 说明 |
|------|------|--------|------|
| FileService | Metadata | dict | 元数据字典 |
| Metadata | Cleaner | dict | 清洗规则配置 |
| Cleaner | AI_Service | dict | 需要评估的字段数据 |
| AI_Service | Cleaner | dict | 评估结果与建议 |
| Cleaner | Storage | list[dict] | 清洗后的记录 |
| Storage | DBService | dict | SQL指令与参数 |
| All | Utils | various | 日志、异常、统计信息 |

---

## 项目初期配置检查清单

根据《文字质量保障体系.md》，实施以下配置：

- [ ] IDE编码设置为UTF-8
- [ ] Python源文件头部添加：`# -*- coding: utf-8 -*-`
- [ ] 所有配置文件使用UTF-8编码
- [ ] 禁止使用Unicode转义替代中文（除非必要）
- [ ] 项目初期全面检查编码配置，不待出现乱码后修复

---

## 数据流向总结

```
临时数据文件 (服务器)
    ↓ [文件操作模块]
本地缓存
    ↓ [元数据处理模块]
清洗规则 ← [业务类型配置]
    ↓ [业务清洗模块]
清洗后数据 ← [质量评分]
    ↓ [数据存储模块]
关系型数据库 (按分层策略自动分片)
    ↓
[日志/报告] ← [工具函数模块]
```

---

## 扩展性设计

### 支持新业务类型的步骤

1. **添加元数据规则配置**：
   - 创建 `config/metadata_rules/{business_type}_rule.json`
   - 定义清洗字段、标准化规则、缺失值处理策略

2. **创建业务类型Cleaner**：
   - 创建 `src/cleaner/cleaners/{business_type}_cleaner.py`
   - 继承 `BaseCleaner` 实现 `clean()` 方法

3. **注册业务类型**：
   - 在 `BusinessTypeRegistry` 中注册新类型ID
   - 在 `CleanerFactory` 中添加创建逻辑

4. **添加单元测试**：
   - 创建 `tests/unit_tests/test_cleaners/test_{business_type}_cleaner.py`

5. **更新文档**：
   - 在 `bussesType/` 目录添加业务类型分析文档
   - 更新主索引文档

### 支持新数据格式的步骤

1. 在 `FileHandler` 中扩展格式支持
2. 在 `FileValidator` 中添加验证规则
3. 在 `QueryBuilder` 中支持新格式的查询

### 支持新数据库的步骤

1. 在 `DatabaseManager` 中添加驱动配置
2. 创建对应数据库的 `QueryBuilder` 子类
3. 更新 `TableSchemaManager` 支持新数据库方言

---

## 编码与质量保障

### 代码规范

- **Python版本**：3.8+
- **编码**：UTF-8，禁止乱码
- **命名**：小驼峰命名法（snake_case）
- **文档**：所有关键函数必须中文注释
- **异常处理**：明确处理每个潜在异常点
- **日志**：关键操作必须记录日志

### 关键代码注释示例

```python
def clean(self, data: List[Dict], metadata: Dict) -> CleaningResult:
    """
    执行完整的数据清洗流程
    
    参数：
        data: 待清洗的原始数据记录列表
        metadata: 数据元数据（包含业务类型、清洗规则等）
    
    返回：
        CleaningResult: 包含清洗后的数据、统计信息、质量评分
    
    实现流程：
        1. 明确清洗目标 - 从元数据获取must_clean_fields
        2. 去重处理 - 按唯一标识去重，删除完全重复
        3. 字段标准化 - 文本、数值、时间、分类等规范化
        4. 缺失值处理 - 根据缺失率决定删除或填充
        5. 异常值修正 - 检测并修复业务逻辑异常值
    """
    # 实现代码...
```

---

## 总结

此目录结构设计遵循以下核心原则：

1. **模块化**：功能职责明确划分，低耦合高内聚
2. **可扩展**：通过配置与工厂模式支持新业务类型
3. **规范化**：严格遵循项目文档规范，特别是编码规范
4. **自动化**：表创建、分片、字段管理尽量自动化
5. **可观测**：完整的日志系统、统计报告、质量评分
6. **可维护**：中文注释、清洗文档、业务类型配置分离

此设计可支持14种核心业务类型，并可轻松扩展至更多类型。
