# 电商商品数据清洗流程规范

## 文档概述

本规范定义了电商商品数据（业务类型_1）的完整清洗流程，包括清洗任务目标、清洗阶段划分、质量控制标准、MySQL数据库表结构设计和字段约束规范。本规范适用于所有从OSS临时存储中获取的原始商品数据，通过标准化流程确保数据质量、一致性和可用性。

**核心原则**：
- 清洗阶段仅负责**格式规范化**，不涉及语义理解
- 所有清洗操作使用**Python纯函数式处理**
- 清洗结果直接入库**MySQL**，为知识构建阶段提供数据源
- 清洗过程记录完整的**操作日志和质量指标**

---

## 第一部分：清洗任务概述

### 1.1 清洗目标定义

电商商品数据清洗的核心目标是将**格式多样、字段不规范、类型混乱的原始数据**转化为**格式统一、字段规范、类型正确的结构化数据**。

**清洗目标三层次**：

```
【清洗深度递进】

第1层：格式规范化 ← 统一日期格式、价格格式、数字格式等
  ↓
第2层：字段映射 ← 不同平台字段映射到标准字段
  ↓
第3层：缺失值处理 ← 缺失数据补充、异常值清理
```

### 1.2 清洗业务流程

```
【清洗完整工作流】

OSS数据获取 → 原始数据加载 → 5阶段清洗 → 质量检查 → MySQL入库 → 生成清洗报告
   ↓            ↓            ↓          ↓         ↓         ↓
获取文件信息   批量加载     逐步规范   数据验证   插入表   统计指标
```

**5阶段清洗流程**：

1. **第一阶段：数据类型转换** - 将所有字段转换为正确的数据类型
2. **第二阶段：格式标准化** - 统一日期格式、价格格式、货币代码等
3. **第三阶段：字段值映射** - 对特定字段进行值映射
4. **第四阶段：缺失值补充** - 对可选字段的缺失值进行合理补充
5. **第五阶段：重复数据去重** - 基于platform + product_id去重

---

## 第二部分：清洗策略与质量控制

### 2.1 分阶段清洗策略

#### 阶段1：数据类型转换

**转换规则表**：

| 源字段 | 源数据类型 | 目标字段 | 目标数据类型 | 转换规则 | 异常处理 |
|-------|----------|---------|----------|---------|---------|
| product_id | Mixed | product_id | VARCHAR(100) | 转为字符串 | 保留原始值 |
| price | String/Float | price | DECIMAL(12,2) | 移除货币符号后转数字 | 标记type_error |
| sales_volume | String/Mixed | sales_volume | INT UNSIGNED | 移除单位后转整数 | 标记type_error |
| comment_count | String/Mixed | comment_count | INT UNSIGNED | 移除单位后转整数 | 标记type_error |
| rating | String/Float | rating | DECIMAL(3,1) | 移除"分"后转浮点 | 标记type_error |
| good_rate | String | good_rate | DECIMAL(5,2) | 移除"%"后转浮点 | 标记type_error |
| update_time | String | update_time | DATETIME | 转换为标准日期 | 标记type_error |

#### 阶段2：格式标准化

**标准化规则表**：

| 字段 | 原始格式示例 | 标准化后格式 | 规范说明 |
|-----|----------|----------|---------|
| price | $9999.00 / ¥9999.00 | 9999.00 | 保留数字和小数点 |
| sales_volume | 15.0万 / 150000 | 150000 | 统一为整数 |
| rating | 4.8分 / 4.8 | 4.8 | 统一为浮点数 |
| good_rate | 96.5% / 96.5 | 96.50 | 统一为百分比格式 |
| update_time | 2024-01-08 15:30:45 / 3天前 | 2024-01-08T15:30:45Z | ISO8601格式 |
| is_official | 官方旗舰店 / 1 / true | 1 | 统一为0/1布尔值 |

#### 阶段3：字段值映射

**映射规则表**：

| 字段 | 映射类型 | 映射规则 | 示例 |
|-----|---------|--------|------|
| source_platform | 平台代码映射 | jd/taobao/suning/pdd统一 | 'jingdong' → 'jd' |
| brand | 品牌名称正规化 | 不规范的品牌名 → 标准品牌名 | 'iphone' → 'Apple' |

#### 阶段4：缺失值处理

**缺失值处理策略**：

| 字段类型 | 字段举例 | 缺失值处理 | 说明 |
|---------|--------|---------|------|
| 必填字段 | product_id, price | 删除该记录或标记incomplete | 无缺失值记录无法使用 |
| 时间字段 | update_time | 补充系统当前时间或设为NULL | 时间有时可推导 |
| 可选字段 | brand, spec_params | 保留NULL | 部分商品无该信息 |

#### 阶段5：重复数据去重

**去重规则**：

| 去重维度 | 比较字段 | 保留策略 | 示例 |
|---------|--------|--------|------|
| 完全重复 | 所有字段相同 | 保留一条 | 同一批数据中 |
| 关键字段重复 | platform + product_id相同 | 保留update_time最新的 | 不同时间抓取 |

### 2.2 质量控制标准

#### 数据验证检查点

**5个关键检查点**：

| 检查点 | 检查项 | 通过标准 | 失败处理 |
|-------|--------|---------|---------|
| 1-类型转换 | 所有字段类型正确 | 转换异常≤1% | 标记type_error并继续 |
| 2-格式规范 | 日期/价格等格式统一 | 格式异常≤1% | 标记format_error或设为NULL |
| 3-值映射 | 字段值在允许值范围 | 映射失败≤2% | 标记unmapped或保留原始值 |
| 4-完整性 | 必填字段无缺失 | 缺失率≤0.5% | 标记incomplete并决定是否删除 |
| 5-一致性 | 记录内字段逻辑一致 | 逻辑矛盾≤1% | 标记inconsistent |

#### 数据质量评估公式

```
清洗质量评分 = 100 
            - (type_error_rate × 20)
            - (format_error_rate × 15)
            - (incomplete_rate × 25)
            - (duplicate_rate × 10)
            - (inconsistent_rate × 20)

质量评级：
  ≥90分：✅优秀，直接入库
  80-89分：⚠️良好，可入库但需标记
  70-79分：🔍一般，需人工审核
  <70分：❌较差，需重新清洗
```

---

## 第三部分：MySQL数据库表结构设计

### 3.1 主表结构：`ec_goods_phone`

**表用途**：存储清洗后的电商商品基础数据

```sql
CREATE TABLE `ec_goods_phone` (
  `goods_id` BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '商品内部ID',
  `product_id` VARCHAR(100) NOT NULL UNIQUE COMMENT '平台商品ID',
  `source_platform` VARCHAR(20) NOT NULL COMMENT '来源平台: jd/taobao/suning/pdd',
  
  `title` VARCHAR(500) NOT NULL COMMENT '商品标题',
  `brand` VARCHAR(100) COMMENT '品牌名称',
  `category` VARCHAR(100) COMMENT '商品分类',
  
  `price` DECIMAL(12, 2) NOT NULL COMMENT '当前销售价格',
  `original_price` DECIMAL(12, 2) COMMENT '原价/市场指导价',
  `currency_code` VARCHAR(3) NOT NULL DEFAULT 'CNY' COMMENT '货币代码',
  
  `spec_params` JSON COMMENT '商品详细规格参数',
  
  `sales_volume` INT UNSIGNED COMMENT '销量',
  `comment_count` INT UNSIGNED COMMENT '评论数',
  `rating` DECIMAL(3, 1) COMMENT '平均评分（0-5）',
  `good_rate` DECIMAL(5, 2) COMMENT '好评率（百分比）',
  
  `shop_name` VARCHAR(100) NOT NULL COMMENT '店铺名称',
  `shop_id` VARCHAR(100) COMMENT '店铺ID',
  `is_official` TINYINT(1) COMMENT '是否官方旗舰店: 0=否 1=是',
  
  `listing_time` DATETIME COMMENT '商品上架时间',
  `update_time` DATETIME NOT NULL COMMENT '商品最后更新时间',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '入库时间',
  `modified_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  
  `quality_flag` VARCHAR(50) COMMENT '数据质量标记',
  `quality_score` DECIMAL(5, 2) COMMENT '质量评分',
  `cleaning_notes` TEXT COMMENT '清洗过程中的注记',
  
  KEY `idx_product_id` (`product_id`),
  KEY `idx_source_platform` (`source_platform`),
  KEY `idx_shop_id` (`shop_id`),
  KEY `idx_brand` (`brand`),
  
  CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci,
  ENGINE=InnoDB
) COMMENT='电商商品数据主表（手机类目）';
```

### 3.2 标签表结构：`ec_goods_phone_tags`

**表用途**：存储商品的标签和分类信息

```sql
CREATE TABLE `ec_goods_phone_tags` (
  `tag_id` BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '标签ID',
  `goods_id` BIGINT NOT NULL COMMENT '商品ID（关联主表）',
  `product_id` VARCHAR(100) NOT NULL COMMENT '平台商品ID',
  
  `tag_category` VARCHAR(50) NOT NULL COMMENT '标签分类',
  `tag_value` VARCHAR(100) NOT NULL COMMENT '标签值',
  `tag_score` DECIMAL(5, 2) COMMENT '标签置信度分数',
  
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '标签生成时间',
  
  KEY `idx_goods_id` (`goods_id`),
  KEY `idx_product_id` (`product_id`),
  KEY `idx_tag_category` (`tag_category`),
  
  CONSTRAINT `fk_goods_id` FOREIGN KEY (`goods_id`) REFERENCES `ec_goods_phone`(`goods_id`) ON DELETE CASCADE,
  
  CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci,
  ENGINE=InnoDB
) COMMENT='电商商品标签表';
```

### 3.3 质量日志表：`ec_goods_cleaning_log`

**表用途**：记录清洗过程中的所有操作日志和质量信息

```sql
CREATE TABLE `ec_goods_cleaning_log` (
  `log_id` BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '日志ID',
  `batch_id` VARCHAR(100) NOT NULL COMMENT '清洗批次ID',
  `batch_date` DATE NOT NULL COMMENT '清洗批次日期',
  
  `total_records` INT NOT NULL COMMENT '该批次总记录数',
  `valid_records` INT NOT NULL COMMENT '有效记录数',
  `invalid_records` INT NOT NULL COMMENT '无效记录数',
  `duplicate_removed` INT NOT NULL DEFAULT 0 COMMENT '去重后移除的记录数',
  
  `type_error_count` INT DEFAULT 0 COMMENT '类型转换错误数',
  `format_error_count` INT DEFAULT 0 COMMENT '格式异常数',
  `missing_error_count` INT DEFAULT 0 COMMENT '缺失值数',
  `overall_quality_score` DECIMAL(5, 2) COMMENT '整批数据质量评分',
  
  `processing_start_time` DATETIME COMMENT '清洗开始时间',
  `processing_end_time` DATETIME COMMENT '清洗结束时间',
  `processing_duration_seconds` INT COMMENT '清洗耗时（秒）',
  
  `db_insert_count` INT DEFAULT 0 COMMENT '插入数据库的记录数',
  `db_status` VARCHAR(50) COMMENT '数据库写入状态',
  
  `processing_notes` LONGTEXT COMMENT '处理过程中的备注',
  `created_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '日志创建时间',
  
  KEY `idx_batch_id` (`batch_id`),
  KEY `idx_batch_date` (`batch_date`),
  
  CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci,
  ENGINE=InnoDB
) COMMENT='电商商品数据清洗日志表';
```

---

## 第四部分：清洗后数据的存储与管理

### 4.1 清洗完成后的存储流程

```
【清洗完成后流程】

清洗完成 → 质量评估 → 分类决策 → MySQL入库 → 生成清洗报告
  ↓         ↓         ↓         ↓        ↓
验证数据   打分评级   分流处理   插入表   统计指标
```

**分流处理规则**：

| 质量等级 | 分数范围 | 处理方式 | MySQL目标 |
|---------|---------|---------|----------|
| ✅优秀 | ≥90 | 直接入库 | ec_goods_phone表 |
| ⚠️良好 | 80-89 | 标记quality_flag后入库 | ec_goods_phone表 |
| 🔍一般 | 70-79 | 标记quality_flag，可能需人工审核 | ec_goods_phone表 |
| ❌较差 | <70 | 标记为quality_flag='poor_quality' | ec_goods_phone表 |

### 4.2 数据分级存储策略

根据数据量级采用不同的处理策略：

| 数据规模 | 数据量级 | 存储策略 | 处理特点 |
|---------|--------|--------|---------|
| 小规模 | <10万 | 全量存储+全量清洗 | 一次性处理 |
| 中规模 | 10-100万 | 分片存储+分片清洗 | 按10万条分片 |
| 大规模 | 100-1000万 | 分片存储+并行清洗 | 按50万条分片 |
| 超大规模 | >1000万 | 分片存储+分布式处理 | 按100万条分片 |

---

## 第五部分：清洗过程异常处理

### 5.1 常见异常与处理方案

| 异常类型 | 具体表现 | 处理方案 | 记录标记 |
|---------|--------|--------|---------|
| 类型转换失败 | 字符串无法转为数字 | 保留原始值，标记为type_error | type_error |
| 格式异常 | 日期格式不符合规范 | 尝试多种格式，失败则设为NULL | format_error |
| 缺失必填字段 | product_id或price为空 | 标记整条记录为incomplete | incomplete |
| 唯一值重复 | product_id重复出现 | 保留最新版本，其他标记为duplicate | duplicate |
| 逻辑矛盾 | price < original_price不成立 | 标记为inconsistent | inconsistent |
| 编码错误 | 中文显示乱码 | 尝试重新编码或保留原始值 | encoding_error |

### 5.2 异常恢复机制

**3层恢复策略**：

```
第1层：记录异常信息
  → 在quality_flag中标记异常类型
  → 在cleaning_notes中记录详细信息

第2层：人工审核（仅对质量评分<70的数据）
  → 导出异常数据供人工检查
  → 决定是否修复或删除

第3层：回滚机制（特定场景）
  → 如某批数据异常率>30%，可标记为需重新处理
  → 保留原始OSS文件，便于重新清洗
```

---

## 第六部分：清洗完成后的验收标准

### 6.1 功能验收清单

- [ ] 所有必填字段完整（缺失率<0.5%）
- [ ] 数据类型正确（type_error_rate<1%）
- [ ] 格式规范一致（format_error_rate<1%）
- [ ] 重复数据已去重（duplicate_rate<0.5%）
- [ ] 数据逻辑一致（inconsistent_rate<1%）
- [ ] 质量评分≥80分
- [ ] MySQL入库成功
- [ ] 清洗日志完整且可追踪

### 6.2 清洗报告标准

```json
{
  "cleaning_report": {
    "batch_id": "EC_GOODS_PHONE_20240108_0001",
    "processing_status": "success",
    "statistics": {
      "total_input_records": 10000,
      "valid_output_records": 9856,
      "invalid_records": 144,
      "quality_score": 92.5
    },
    "error_breakdown": {
      "type_error_count": 23,
      "format_error_count": 45,
      "missing_value_count": 76,
      "duplicate_count": 89,
      "inconsistent_count": 12
    },
    "database_status": {
      "inserted_records": 9856,
      "status": "success"
    }
  }
}
```

---

**文档版本**：1.0  
**最后更新**：2024年1月  
**维护人员**：数据清洗团队
