# Python MCP 数据清洗服务项目规范

## 1. 项目概述

本项目是一个基于 Python 的 MCP（数据处理管道）服务，专门用于数据清洗。项目旨在通过标准化的流程处理来自不同业务场景的数据，将原始数据转换为结构化的数据并存储到数据表中。

## 2. 项目架构与功能范围

### 2.1 核心功能

本服务严格限定在以下三个核心功能范围内：

1. **获取临时数据文件** - 从指定位置读取临时数据文件
2. **数据清洗** - 基于元数据规则对数据进行标准化处理
3. **存放到数据表** - 将清洗后的数据存储到数据库表中

### 2.2 功能边界

本服务**不包含**以下功能：

- 数据抓取/爬取
- 用户界面
- 数据可视化
- 任务调度
- 数据备份与恢复
- 权限管理
- API 接口（除内部处理接口外）

## 3. 数据处理流程

### 3.1 数据三级属性体系

本项目采用数据三级属性体系进行数据分类管理：

| 层级               | 核心定义                              | 作用                     | 示例                                                         |
| ------------------ | ------------------------------------- | ------------------------ | ------------------------------------------------------------ |
| 一级：核心业务类型 | 数据对应的"具体实体/行为"，不可再拆分 | 明确数据清洗的目标与标准 | 电商商品、社交平台评论、资讯文章                             |
| 二级：数据载体类型 | 数据的物理存储形式                    | 确定数据处理的工具选择   | JSON 接口数据、PDF 文档、网页 HTML                           |
| 三级：数据结构类型 | 数据的组织形式                        | 确定数据处理的基础方法   | 结构化（JSON/CSV）、半结构化（HTML/XML）、非结构化（纯文本） |

### 3.2 元数据规则配置

基于不同业务场景，通过元数据规则配置文件定义数据清洗标准：

```json
{
  "business_type_id": "唯一业务类型ID（如EC_GOODS_PHONE）",
  "business_type_name": "核心业务类型名称",
  "business_description": "业务类型描述",
  "clean_config": {
    "must_clean_fields": ["必须清洗的核心字段"],
    "field_standard_rule": {
      "字段名": "标准化规则（如格式转换、数据类型等）"
    },
    "missing_value_handle": {
      "核心字段": "缺失值处理策略",
      "扩展字段": "缺失值处理策略"
    },
    "abnormal_value_handle": "异常值处理规则",
    "data_quality_check": "数据质量校验规则"
  },
  "storage_config": {
    "database_config": {
      "table_name": "目标数据库表名",
      "primary_key": "主键字段",
      "field_mapping": "字段映射关系"
    }
  }
}
```

## 4. 数据存储规范

### 4.1 临时数据存储

- 所有临时数据必须以**成对文件**形式存储在阿里云服务器上：

  1. **正式数据文件**（如 JSON、CSV 等）：包含实际的数据内容
  2. **元数据 JSON 文件**：与正式文件同名，仅后缀为`.metadata.json`，包含数据的关键信息

- 文件命名规范：`{业务类型}_{日期}_{序号}.{扩展名}`

  - 正式文件示例：`EC_GOODS_PHONE_20240108_0001.json`
  - 元数据文件示例：`EC_GOODS_PHONE_20240108_0001.metadata.json`

- 元数据文件结构：

```json
{
  "business_type_id": "EC_GOODS_PHONE",
  "data_structure_type": "结构化",
  "file_size_mb": 50,
  "data_carrier_type": "JSON",
  "estimated_record_count": 10000,
  "crawl_date": "20240108",
  "data_source": "京东商城",
  "actual_record_count": 9856,
  "crawl_tool": "Scrapy",
  "checksum": "abc123def456" // 文件完整性校验值
}
```

- 临时数据保留策略：处理完成后保留 7 天，之后自动清理

### 4.2 最终数据存储

- 清洗后的数据存储到关系型数据库中
- 数据库表结构与业务类型配置文件中定义的映射关系一致
- 支持主流数据库：MySQL、PostgreSQL、SQLite

## 5. 数据清洗核心逻辑

无论处理何种数据类型，清洗流程均遵循以下 5 个核心步骤：

### 5.1 明确清洗目标

基于元数据规则配置，确定需要清洗的字段和标准，聚焦核心字段，避免无效处理。

### 5.2 去重处理

按唯一标识（如商品 ID、评论 ID、文章 ID）进行去重，优先处理完全重复记录。

### 5.3 字段标准化

- 文本字段：去空格、去特殊字符、统一大小写
- 数值字段：去除单位、转为标准数字型
- 时间字段：统一转为标准时间格式（YYYY-MM-DD HH:MM:SS）
- 标签字段：相似标签归一化

### 5.4 缺失值处理

- 核心字段：缺失率较低时直接删除记录，缺失率较高时标注为"待补充"
- 非核心字段：填充为"未知"或使用众数、中位数填充

### 5.5 异常值修正

- 数值异常：通过业务逻辑判断合理范围，用中位数或平均值修正
- 格式异常：尝试修复格式，无法修复则删除或标注
- 内容异常：直接剔除无意义内容

## 6. 技术要求

### 6.1 开发语言

- Python 3.8+

### 6.2 核心依赖库

- pandas：结构化数据处理
- numpy：数值计算
- SQLAlchemy：数据库操作
- json：JSON 数据处理
- datetime：时间处理
- re：正则表达式处理

### 6.3 文件格式支持

- 输入格式：JSON、CSV、TXT
- 输出格式：数据库表记录

## 7. 项目目录结构

```
project_root/
├── config/                 # 配置文件目录
│   ├── metadata_rules/     # 元数据规则配置文件
│   └── database_config.py  # 数据库连接配置
├── data/                   # 数据文件目录
│   ├── temp/              # 临时数据文件
│   └── processed/         # 已处理数据记录
├── logs/                   # 日志文件目录
├── src/                    # 源代码目录
│   ├── cleaner/           # 数据清洗模块
│   ├── metadata/          # 元数据处理模块
│   ├── storage/           # 数据存储模块
│   └── utils/             # 工具函数模块
├── tests/                  # 测试文件目录
└── main.py                 # 主程序入口
```

## 8. 错误处理与日志

### 8.1 错误处理

- 数据读取失败：记录错误日志，跳过当前文件，继续处理其他文件
- 清洗过程异常：记录详细错误信息，标记数据为"清洗失败"
- 数据库存储异常：回滚操作，记录错误信息

### 8.2 日志规范

- 日志级别：DEBUG、INFO、WARNING、ERROR
- 日志格式：[时间戳] [级别] [模块名] [消息内容]
- 日志保留：最近 30 天日志

## 9. 性能要求

- 单次处理数据量：支持 10 万条记录以下的数据批量处理
- 处理速度：1 万条记录处理时间不超过 30 秒
- 内存使用：处理过程中内存占用不超过 2GB

## 10. 扩展性要求

- 通过配置文件支持新业务类型，无需修改核心代码
- 模块化设计，便于功能扩展
- 支持插件式数据清洗规则

## 11. MCP 服务请求接口设计

### 11.1 整体架构

MCP 服务是独立的处理单元，各个 MCP 之间仅需谓解上游输出的**临时数据文件位置**。外部 Agent 不需要了解每个 MCP 的内部处理逻辑，只需调用下一个 MCP 的输入接口及输出位置。

**流程示例：**

```
抓取MCP -> 清洗MCP -> 打标MCP
    |输出位置
    v
临时数据文件(正式+元数据)
    |输入位置
    v
  清洗MCP
    |输出位置
    v
  清洗后数据文件 + 数据库
```

### 11.2 清洗服务请求参数

清洗服务的请求参数精简，仅需指定临时数据文件的路径：

```json
{
  "file_paths": [
    "/aliyun/data/EC_GOODS_PHONE_20240108_0001.json",
    "/aliyun/data/EC_GOODS_PHONE_20240108_0002.json"
  ]
}
```

**参数说明：**

- `file_paths`：文件路径数组，支持单个或多个文件处理
- 服务端会自动查询同名的`.metadata.json`文件获取元数据信息
- 不需要指定业务类型、数据结构类型等信息，服务端从元数据文件自动获取

### 11.3 服务端处理流程

1. **加载元数据：**根据`file_path`自动查找同名的`.metadata.json`文件
2. **获取元数据信息：**
   - `business_type_id`：捹取对应的清洗规则配置
   - `data_structure_type`：判断数据结构，选择处理工具
   - `file_size_mb`：判断是否需要分批处理
   - `estimated_record_count`：优化批处理参数
3. **自动优化策略：**根据元数据自动选择最优的处理配置
4. **执行清洗：**按最优策略执行数据清洗、去重、正一化、潛在值、灰常值处理
5. **保存结果：**
   - 将清洗后的数据保存到数据库
   - 输出新的清洗数据文件位置（供下一个 MCP 使用）

### 11.4 服务端处理配置

```json
{
  "processing_strategy": {
    "file_size_thresholds": {
      "small": 10, // 小文件阈值（MB）
      "medium": 500 // 中等文件阈值（MB）
    },
    "record_count_thresholds": {
      "small": 10000, // 小数海量记录阈值
      "medium": 100000 // 中等数海量记录阈值
    },
    "tools": {
      "structured_data": {
        "small": "pandas",
        "medium": "pandas_chunked | dask",
        "large": "dask"
      },
      "semi_structured_data": "beautifulsoup4 + regex",
      "unstructured_data": "regex | tesseract-ocr"
    },
    "batch_configs": {
      "small": {
        "batch_size": 1000,
        "parallel": false
      },
      "medium": {
        "batch_size": 5000,
        "parallel": true,
        "max_workers": 4
      },
      "large": {
        "batch_size": 10000,
        "parallel": true,
        "max_workers": 8,
        "use_dask": true
      }
    }
  },
  "business_type_configs": {
    "EC_GOODS_PHONE": {
      "data_structure_type": "结构化",
      "preferred_tool": "pandas",
      "batch_size": 5000
    },
    "SOCIAL_MEDIA_COMMENT": {
      "data_structure_type": "半结构化",
      "preferred_tool": "beautifulsoup4",
      "batch_size": 1000
    }
  },
  "default_timeout": 300,
  "async_processing_enabled": true
}
```

**配置设计提示：**

- 服务端根据这些配置自动优化处理策略，不需要请求端指定
- 联合业务类型配置和文件元数据，选择最优处理工具
- 支持异步处理，大型批量数据可打提批次处理

## 12. 文字质量保障体系 - 编码和乱码防控规范

### 12.1 核心原则

**预防优于修复：从源头避免乱码问题**

### 12.2 第一阶段：项目初期配置

- **全面检查和修改文件编码配置**：在项目配置阶段全面修改关键文件和配置，确保 IDE/编辑器、构建工具的默认编码均设置为 **UTF-8**。
- **时机要求**：此项工作必须在项目开始时完成，禁止等到出现乱码问题后再行修复。

### 12.3 第二阶段：代码生成和编辑

- **中文编写原则**：优先直接使用中文编写文本，完全停止滥用 Unicode 转义作为“保险”手段。
- **Unicode 转义限制**：仅在极少数无法通过配置解决的特殊字符场景下考虑使用。
- **标准流程**：
  1. 确保 IDE 配置为 UTF-8。
  2. 直接编写中文文本。
  3. 验证生成结果。
  4. 出现乱码时，修复根本原因（配置或转码），严禁简单删除。

### 12.4 第三阶段：问题修复处理策略

- **修复而非删除**：严禁通过删除乱码来掩盖问题，必须识别来源（转码或配置问题）并用正确文本替换。
- **质量检查标准**：
  - IDE/构建工具编码：UTF-8。
  - 代码输入：直接输入中文，无滥用转义。
  - 生成验证：中文显示正常，无损业务信息。
